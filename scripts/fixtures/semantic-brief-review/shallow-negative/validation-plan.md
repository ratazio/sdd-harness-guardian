# Validation — fictional shallow-negative source

Run the two-tenant integration probe. The oracle is that no foreign invoice ID
is returned. Evidence belongs in `evidence/T-003.md` before a release decision.
