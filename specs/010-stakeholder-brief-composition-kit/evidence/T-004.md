# Evidence Pack: T-004 — source-to-brief parity and reviewer guidance

**Task state:** needs_evaluation  
**Builder:** platform-engineering  
**Evaluator:** sandbox_coverage_review (distinct)  
**Date:** 2026-08-26

## Delivered increment

`scripts/test_brief_composition_contract.py` compares populated source task IDs
with the Execution panel and populated AC IDs with Validation. The rich fixture
passes; the scaffold fixture fails for all four task IDs, all four AC IDs and
the forbidden populated-scaffold lifecycle. The check compares identifiers
only: it does not rate prose, usability, architecture or visual quality.

The authoring guidance now requires this parity check before a derived brief is
linked/presented, and defines `data-brief-phase="scaffold"` as outside Human
Visibility and Tasks Ready.

## Commands and results

- `python scripts/test_brief_v2_contracts.py` — pass.
- `python scripts/test_tabbed_brief_surface.py` — pass.
- `python scripts/test_semantic_brief_review_calibration.py` — pass; semantic
  adequacy still requires independent review.
- `python scripts/test_brief_composition_contract.py` — pass.
- `python scripts/validate_bundle.py` — pass (267 checks).
- `python scripts/validate_human_visibility.py --consumer-root . --initiative
  specs/010-stakeholder-brief-composition-kit` — pass after regenerated brief
  and freshness baseline.

## Exit-criteria check

- Rich tasks/ACs have deterministic ID parity: yes.
- Generic populated scaffold is rejected: yes.
- No semantic score, profile coupling or post-render fixer: yes.
- Brief/state/evidence synchronized: yes at submission.
- Distinct evaluation: pending.
