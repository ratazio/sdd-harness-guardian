# Evidence: T-005 — Release validation and independent-rendered-review handoff

**Task state:** needs_evaluation  
**Builder:** `spec012_t005_release_builder`  
**Evaluator:** pending — must be distinct from the builder  
**Date:** 2026-08-27

## Delivered increment

The release builder repeated the local deterministic suite, bundle validation,
Python compilation and whitespace check. It refreshed and rechecked the
existing repaired consumer fixture and SPEC 012. It also generated a second,
clean synthetic consumer initiative at
`testes/specs/002-kiosk-accessibility-journey`.

The second fixture deliberately differs from the blog fixture: it specifies a
single-device accessibility journey with browser-local state, no server, no
HTTP/API route and no formal `IR-*` register. Its v2 brief has the canonical
eight decision panels, task-ledger projection, source coverage/provenance,
keyboard tabs, a no-script message, responsive rules and print visibility.

## Commands and results

| Command | Result |
|---|---|
| `python scripts/test_validate_human_visibility.py` | PASS (`RESULT: PASS`). |
| `python scripts/validate_bundle.py` | PASS (`Bundle validation passed: 267 checks.`). |
| `python -m py_compile scripts/validate_human_visibility.py scripts/test_validate_human_visibility.py` | PASS (exit 0). |
| `git diff --check -- .harness/templates .harness/rules/human-visibility.md scripts/validate_human_visibility.py scripts/test_validate_human_visibility.py specs/012-stakeholder-brief-evidence-projection-enforcement testes/specs` | PASS (exit 0). |
| `python scripts/validate_human_visibility.py --consumer-root testes --initiative specs/001-news-blog-auth --write-baseline` then recheck | PASS; repaired fixture baseline synchronized. |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/012-stakeholder-brief-evidence-projection-enforcement --write-baseline` then recheck | PASS; SPEC 012 baseline synchronized. |
| `python scripts/validate_human_visibility.py --consumer-root testes --initiative specs/002-kiosk-accessibility-journey --write-baseline` then recheck | PASS; new different-shape fixture baseline synchronized. |

## Static rendered-review preparation

| Review point | Result / boundary |
|---|---|
| v2 structure | The eight required panel IDs, canonical shell hooks and human coverage table are present. |
| Provenance | All eight canonical sources have represented/synthesized coverage rows and target IDs. |
| Different-shape compatibility | No `IR-*` table rows and no HTTP `/api/` contracts occur, so projection obligations are correctly absent rather than fabricated. |
| No-script and tabs | `noscript` text preserves the one-document fallback; role tabs select panels through unobtrusive script and remain anchors when script is absent. |
| Print and responsive | Print reveals hidden panels; narrow-view CSS collapses the grid and permits table overflow. |
| Limit | Static inspection and deterministic checks do not establish visual legibility, assistive-technology behavior or decision usefulness. |

## Evaluator request

Review the actual rendered repaired and local-kiosk briefs, the diagnostic
boundary and the retained distinction between deterministic integrity and
semantic approval. Decide `approve` or `needs_revision`, record residual risk,
and do not edit the implementation while evaluating. The builder has not and
cannot approve T-005.

## Builder revision after evaluator finding

The evaluator returned `needs_revision` because the second fixture had only a
minimal click-based tab enhancement. The fixture now implements the canonical
tab contract without changing its local-only domain shape:

- every tab has a stable `tab-*` ID and `aria-controls`; every panel has the
  matching `aria-labelledby` and `tabindex="0"`;
- activation sets `aria-selected`, `hidden` and roving `tabIndex` together;
- ArrowLeft, ArrowRight, Home, End, Space and Enter are handled, activation
  follows/updates the hash, and hash changes select the matching panel;
- before print reveals all panels and after print restores the active panel;
  anchors and the no-script one-document fallback remain intact.

The builder again ran `node --check` on the extracted inline script, the
focused suite, bundle validation (267 checks), Python compilation, the new
fixture baseline write/recheck and scoped `git diff --check`; all passed. This
repair returns T-005 to `needs_evaluation`; it does not approve it.

## Evaluator decision

**Decision:** pending — prior evaluator requested tab-contract revision; builder repair awaits fresh distinct evaluation  
**Evaluator:** pending  
**Date:** pending  
**Findings / residual-risk disposition:** pending
