# Evidence: T-004 — Synchronize templates, fixture and baseline checks

**Task state:** needs_evaluation  
**Builder:** `spec012_t004_builder2`  
**Evaluator:** pending — must be distinct from the builder  
**Date:** 2026-08-27

## Delivered increment

The v2 authoring contract now makes the evidence lifecycle explicit without
changing the T-002 safe resolver:

- `.harness/templates/README.md`, `run-state.yaml.md` and the Human Visibility
  rule state that a declared future `evidence/T-XXX.md` is deferred only for a
  canonical block-form task-ledger entry in `pending`, `ready`, `in_progress`
  or `blocked`;
- the real synthetic consumer fixture uses that canonical block form for its
  five planning-only tasks, so no fabricated task evidence is needed before a
  fictitious owner selects a task; and
- the focused suite proves the complete boundary: the same absent
  `evidence/T-203.md` passes while `pending`, `ready`, `in_progress` and
  `blocked`, then fails with its source-specific diagnostic at
  `needs_evaluation`, `approved` and `done`; every state is asserted in both
  normal validation and `--write-baseline` mode.

The change does not permit absolute/traversal locators, read another initiative,
defer evidence in evaluation/terminal states, or turn deterministic PASS into
an evaluator decision.

## Acceptance and exit-criteria coverage

| Claim / AC or exit criterion | Proof | Result |
|---|---|---|
| AC-004 / repaired fixture passes strengthened validator and baseline recheck | Fixture Human Visibility write then recheck. | PASS |
| AC-005 / v2 and compatibility controls keep intended behavior | Focused fixture suite covers all four deferred states and all three evidence-required states in normal and baseline modes. | PASS |
| AC-007 / guidance retains distinct semantic/rendered review | Template and rule text explicitly retain independent review after deterministic PASS. | Implemented; evaluator must confirm. |
| V-REG-002 / baseline only after integrity checks | Each of `needs_evaluation`, `approved` and `done` fails normal validation and `--write-baseline` when the declared pack is absent; all four future states pass in both modes. | PASS |
| V-REG-003 / deterministic local repeatability | Focused suite and bundle run locally without network. | PASS |
| Exit / state synchronized, evaluator still distinct | This evidence, tasks, state, progress, decision log, handoff and derived brief are synchronized. | PASS; approval pending |

## Commands and results

| Command | Result |
|---|---|
| `python scripts/test_validate_human_visibility.py` | PASS (`RESULT: PASS`), including normal and baseline assertions for `pending`/`ready`/`in_progress`/`blocked` deferment and `needs_evaluation`/`approved`/`done` required-evidence controls. |
| `python scripts/validate_human_visibility.py --consumer-root testes --initiative specs/001-news-blog-auth --write-baseline` | PASS; wrote fixture baseline. |
| `python scripts/validate_human_visibility.py --consumer-root testes --initiative specs/001-news-blog-auth` | PASS; no structural, gate/state or freshness failure. |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/012-stakeholder-brief-evidence-projection-enforcement --write-baseline` | PASS; wrote SPEC 012 baseline. |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/012-stakeholder-brief-evidence-projection-enforcement` | PASS; no structural, gate/state or freshness failure. |
| `python scripts/validate_bundle.py` | PASS (`Bundle validation passed: 267 checks.`). |
| `python -m py_compile scripts/validate_human_visibility.py scripts/test_validate_human_visibility.py` | PASS (exit 0). |
| `git diff --check -- .harness/templates/README.md .harness/templates/run-state.yaml.md .harness/rules/human-visibility.md scripts/test_validate_human_visibility.py specs/012-stakeholder-brief-evidence-projection-enforcement testes/specs/001-news-blog-auth` | PASS (exit 0). |

## Files changed

- `.harness/templates/README.md`
- `.harness/templates/run-state.yaml.md`
- `.harness/rules/human-visibility.md`
- `scripts/test_validate_human_visibility.py`
- `testes/specs/001-news-blog-auth/run-state.yaml`
- `testes/specs/001-news-blog-auth/human-visibility-baseline.json`
- `specs/012-stakeholder-brief-evidence-projection-enforcement/human-visibility-baseline.json`
- T-004 lifecycle artifacts and this evidence pack.

## Known gaps and residual risk

| Gap / risk | Severity | Owner / follow-up | Blocking? |
|---|---|---|---|
| The deferred task ledger parser intentionally recognizes the documented block-item contract, not every equivalent YAML flow-style representation. | low | independent evaluator validates that author guidance matches the supported contract. | no |
| Deterministic PASS cannot judge whether guidance is sufficiently clear or a rendered brief is decision-useful. | medium | T-004 evaluator and T-005 semantic reviewer. | yes — evaluation gate only |

## Evaluator request

Confirm that the documented ledger form accurately bounds the implementation;
that all four future states defer and all three evaluation/terminal states make
missing evidence mandatory; that the fixture does not fabricate proof; and
that no wording weakens containment, independent evaluation or
semantic/rendered-review requirements. Record `approve` or `needs_revision`
below. The builder must not approve this task.

## Evaluator decision

**Decision:** approve  
**Evaluator:** `spec012_t004_evaluator2`  
**Date:** 2026-08-27  
**Findings / residual-risk disposition:** All four future states defer and all
three evaluation/terminal states require the pack in both normal and baseline
modes. Guidance remains bounded to canonical ledger blocks, containment is
retained, and deterministic PASS is not semantic approval.
