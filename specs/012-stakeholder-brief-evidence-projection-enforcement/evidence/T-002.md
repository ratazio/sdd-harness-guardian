# Evidence: T-002 — Enforce safe referenced-evidence existence

**Task state:** needs_evaluation  
**Builder:** `spec012_t002_builder`  
**Evaluator:** pending — must be distinct and perform the required security review  
**Date:** 2026-08-27

## Delivered increment

`scripts/validate_human_visibility.py` now scans the canonical v2 source set
for cited `evidence/*.md` paths. It removes optional `#fragment` anchors before
file resolution, rejects absolute/traversal/non-initiative paths before an
existence read, and reports the canonical source plus missing relative file.
The check runs through `validate()` and therefore blocks `--write-baseline`.

It intentionally does not inspect v1 sources, arbitrary links, evidence
content, risks, or API-route projection; those boundaries preserve legacy
compatibility and retain T-003 as a separate increment.

## Validation performed

| Command | Result | What it proves |
|---|---|---|
| `python -m py_compile scripts/validate_human_visibility.py scripts/test_validate_human_visibility.py` | PASS | Changed Python modules parse. |
| `python scripts/test_validate_human_visibility.py` | PASS (`RESULT: PASS`) | Valid anchored evidence, missing artifact, traversal, absolute path, v1 compatibility, and existing deterministic validator regressions. |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/012-stakeholder-brief-evidence-projection-enforcement` | Structural/gate PASS; expected freshness re-review required | The synchronized brief has valid structure and provenance; its changed reviewed baseline is intentionally not rewritten by its builder. |
| `git diff --check -- scripts/validate_human_visibility.py scripts/test_validate_human_visibility.py` | PASS | No whitespace errors in task-owned implementation/tests. |

## Security and lifecycle notes

- The resolver validates `Path.is_absolute()`, `..` components, `evidence/`
  root and resolved containment before `is_file()`.
- A diagnostic does not include source-file contents or attempt a read outside
  the initiative.
- This builder has not approved the task. A distinct evaluator must inspect
  path parsing, Windows absolute-path behavior, symlink containment, the
  baseline ordering, and regression scope before D-010 can approve T-002.

## Changed files

- `scripts/validate_human_visibility.py`
- `scripts/test_validate_human_visibility.py`
- `tasks.md`, `run-state.yaml`, `progress.md`, `decision-log.md`,
  `handoffs/latest-handoff.md`, and `stakeholder-brief.html` (T-002 state
  synchronization only)

## Independent security evaluation

`spec012_t002_evaluator` returned **needs revision**: `./evidence/review.md` was not captured by either evidence-reference pattern, so an absent file could still permit `--write-baseline`. Normalize/capture this prefix (or reject it explicitly) and add a regression proving baseline failure before requesting re-evaluation. All other tested controls passed.

## Builder repair after evaluator finding

The standard evidence-reference grammar now accepts an optional `./` (or
`.\\`) prefix and normalizes it to the canonical initiative-relative form
before deferred-task and filesystem checks. A regression proves that a missing
`./evidence/missing-dot-prefix.md#final` fails both normal validation and
`--write-baseline` with the canonical path in the diagnostic.

Re-ran after repair:

| Command | Result |
|---|---|
| `python -m py_compile scripts/validate_human_visibility.py scripts/test_validate_human_visibility.py` | PASS |
| `python scripts/test_validate_human_visibility.py` | PASS (`RESULT: PASS`) |
| `git diff --check -- scripts/validate_human_visibility.py scripts/test_validate_human_visibility.py specs/012-stakeholder-brief-evidence-projection-enforcement` | PASS |

This is a repair handoff, not an approval. Request a new distinct security
evaluation of the `./` normalization and retained containment controls.
