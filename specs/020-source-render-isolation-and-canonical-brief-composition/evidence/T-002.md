# Evidence — T-002

**Builder:** codex-builder-020 · **Evaluator:** /root/t002_final_evaluation · **Date:** 2026-08-28 · **Status:** approved

## Entrega

`render_stakeholder_brief.py` promove apenas candidato v2 composto e revisado:
o record indicado em `brief_review.review_record` resolve uma seção de decisão
exata, liga autor/revisor/outcome, SHA-256 do candidato e digests atuais das
fontes. Cada bloco declarado carrega `data-source`, locator, coverage e digest
local; o record de decisão usa digest normalizado para não criar ciclo de hash.
Uma topologia de proveniência idêntica à casca canônica é recusada mesmo com
atributos, SHA e digests corretos. Isto verifica linhagem, não prosa.

Pearson é estritamente opt-in: só o root
`data-client-identity-profile="pearson"` ativa a política e provisiona o asset
local aprovado. O padrão `vendor-neutral` não copia, carrega ou referencia esse
asset. Human Visibility continua bloqueado até revisão qualitativa pós-render.

## Validação

| Validation | Resultado |
|---|---|
| V-003 `python scripts/test_render_stakeholder_brief.py` | PASS — SHA em D-901, casca reclassificada com SHA/record/digests por bloco, digest stale, fonte externa, hotlink, HTML precoce e overwrite recusados; candidato composto e Pearson selecionado promovem. |
| V-004 `python scripts/test_client_identity_profile_contract.py` | PASS — padrão neutro sem referência Pearson; seleção explícita/local preservada. |
| V-004 `python scripts/test_client_identity_profile_render.py` | PASS — HTTP local: neutro sem request/DOM Pearson; perfil selecionado com foco, teclado, no-script, print, responsividade, zoom e reduced motion. |
| V-003 `python scripts/test_decision_quality_review_contract.py` | PASS — integridade do registro; sem score semântico. |
| Regressão `python scripts/test_validate_human_visibility.py` | PASS — rendering não substitui revisão de qualidade. |
| Bundle `python scripts/validate_bundle.py` | PASS — 271 checks. |
| Política adicional `python scripts/test_pearson_brief_policy.py` | PASS — neutral default e enforcement Pearson explícito. |

## AC e limite

AC-002 satisfeito. O promotor prova integridade de record, fonte e perfil, mas
não mede qualidade narrativa, arquitetura, diagramas, abas ou volume de texto.
A suficiência decisória permanece julgamento humano obrigatório.

## Avaliação independente

Dois pareceres independentes anteriores retornaram `REVISE` e bloquearam T-002:
primeiro por casca com atributos declarativos; segundo por casca com todos os
digests por bloco. Ambos os bypasses viraram regressões. O parecer final de
`/root/t002_final_evaluation` foi `APPROVE`: ele reproduziu em raiz temporária
o ataque completo (SHA/record/autor/revisor/outcome/digests e placeholders
trocados) e obteve a recusa por topologia de proveniência do scaffold. Nenhum
achado material permaneceu; builder e avaliador são distintos.
