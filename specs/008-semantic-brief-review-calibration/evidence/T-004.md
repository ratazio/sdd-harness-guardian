# Evidence: T-004 — independent rendered-package validation

**Initiative:** 008-semantic-brief-review-calibration  
**Builder:** Codex / builder-008  
**Evaluator:** `/root/sandbox_coverage_review`  
**Decision:** approved — T-004 and release accepted in D-009

## Package reviewed

The evaluator independently reviewed the software and non-software calibration
fixtures and the end-to-end fictitious `001-news-blog-auth` initiative. The
sandbox reviewer returned a pre-render correction (D-005), pre-render PASS
(D-006), rendered correction (D-007), rendered PASS/Human Visibility (D-008)
and did not grant Tasks Ready. The builder then recorded D-009 as the explicitly
allowed fictitious meeting/progression decision: only T-001 became `ready`; no
task entered `in_progress` and no application code was created.

| Validation | Result | Evidence |
|---|---|---|
| V-001–004 / fixture reviews | PASS | Three review records under `scripts/fixtures/semantic-brief-review/`. |
| V-005–006 / boundary and v1 | PASS | T-003 checks. |
| M-001–003 / rendered meaning and accessibility structure | PASS with local screenshot limitation | D-007/D-008 and sandbox `evidence/planning-review.md`. |
| V-007 / release review | PASS | T-004 and release accepted by the distinct evaluator in D-009. |

| Command | Result |
|---|---|
| `python scripts/validate_human_visibility.py --consumer-root testes/news-blog-spec-sandbox --initiative specs/001-news-blog-auth --write-baseline` | PASS — current v2 baseline written after review/propagation. |
| Same validator without `--write-baseline` | PASS — no structural, gate or freshness failure. |
| `python scripts/test_semantic_brief_review_calibration.py` | PASS. |
| `python scripts/test_brief_v2_contracts.py` | PASS. |
| `python scripts/test_validate_human_visibility.py` | PASS. |
| `python scripts/validate_bundle.py` | PASS — 267 checks. |

## Independent evaluation and corrective action

The evaluator approved the package content but returned this task in D-008:
the 008 validator still could not link Tasks Ready to a propagated decision,
its fresh baseline was absent, and the rendered header was stale. The builder
updated the review-record locator to D-007 (which records the propagated
decision) and refreshed the header. The evaluator rechecked the package,
approved T-004 and release in D-009, and authorized baseline creation. No
screenshot is retroactively asserted.

## Terminal validation

D-010 records the post-approval baseline write and a clean non-write Human
Visibility validation, alongside passing calibration, brief-contract, validator
fixture and bundle checks. The validator's standard reminder that independent
review cannot be automated is satisfied by the distinct D-008/D-009 decisions.

## Known limitation

Browser Use blocked direct navigation to local HTML, so no screenshot was
captured or claimed. The HTML responsive/no-script/print contract was inspected
statically and the independent reviewer assessed the decision surface from the
browser-independent artifact. This is non-blocking for the static bundle change
but remains visible for a future manual local visual check.
