# Evidence: T-003 — structural-only boundary and v1 compatibility

**Initiative:** 008-semantic-brief-review-calibration  
**Builder:** Codex / builder-008  
**Evaluator:** `/root/sandbox_coverage_review`  
**Decision:** approved — D-008

## Delivered increment

No change added a score, word threshold, prose parser, LLM judge, semantic
schema or deterministic semantic approval. The production Human Visibility
validator remains untouched; guidance preserves v1 lineage. The new fixture
script only checks fixture presence/record locators and says it does not judge
prose quality.

| AC / exit criterion | Result | Validation |
|---|---|---|
| AC-005 | Implemented | Diff review: guidance, docs, static fixtures and locator test only; no semantic validator change. |
| AC-006 | Implemented | Existing v1/v2 compatibility suite passes. |
| FR-007 | Implemented | No mirror was introduced, so no need/cost/removal record was fabricated. |

| Check | Result |
|---|---|
| `python scripts/test_brief_v2_contracts.py` | PASS — includes legacy v1 lifecycle fixture. |
| `python scripts/test_validate_human_visibility.py` | PASS — v1 and v2 consumer validator fixtures. |
| `python scripts/validate_bundle.py` | PASS — 267 deterministic bundle checks. |

**Residual risk:** a future semantic mirror needs new evidence and authority.

## Independent evaluation

The distinct evaluator approved this boundary: no score, parser, LLM judge or
semantic gate was added, and the existing v1 lineage checks remain valid. See
`decision-log.md#D-008`.
