# Evidence Draft — T-001

**Status:** approved / task done. **Builder:** `spec021_t001_builder`.
**Evaluator:** `/root/reevaluate_spec021_t001` (independent). **Approved:**
2026-08-30.

## Delivered increment

- Added `.harness/templates/conditional-source-semantic-review.md`: a
  corpus-driven human-review record with reviewer-declared inputs, conditional
  ratchet states, source/candidate locators, impact, repair, rerender,
  re-review, and source-backed N/A rationale.
- Added material-ratchet, justified-empty-ratchet, and omitted-relation
  fixtures plus `scripts/test_conditional_source_hook.py`.
- Repaired the reviewer records after independent feedback: all three now
  declare the effective corpus as path, locator/scope, and digest; the two
  ratchet HTML fixtures bind their `data-source-digest` to the exact source.
- Second repair: the conditional-inventory provenance rows now reuse the
  exact `ratchet.md` SHA-256 from their declared manifests, and the focused
  test rejects a provenance row that is not bound to that local source.

## AC / validation mapping

| Acceptance / validation | Draft proof |
|---|---|
| AC-021-01 / V-021-01 | The material fixture recovers trigger, check, owner, consequence and ratchet provenance. The empty fixture recovers empty state and a cited reason. |
| AC-021-02 / V-021-02 | The omitted-relation fixture requires a decision impossible from HTML, source and candidate locators, impact, concrete repair/rerender/re-review, plus a source-backed N/A. |

## Commands to evaluate

```text
python scripts/test_conditional_source_hook.py
python scripts/test_semantic_brief_review_calibration.py
python scripts/validate_bundle.py
```

**Latest verified run after the corpus-manifest repair:** all three commands
passed; bundle validation reported 272 checks. This proves record wiring and
identity binding only, not semantic sufficiency.

## Boundary preserved

The new test is static fixture wiring. It does not determine materiality,
recognize a domain, score text or visuals, choose a representation, or approve
a semantic review.

## Independent evaluation

`/root/reevaluate_spec021_t001` returned **APPROVE** on 2026-08-30. The
evaluator confirmed that the three review records bind their effective corpus
by path, locator/scope and digest; the material ratchet makes trigger, check,
owner, consequence and provenance recoverable; the empty ratchet states its
reason; and the hook requires an impossible decision, locators, impact,
repair and justified N/A without becoming a taxonomy, score or automatic
approval. This approval closes T-001 only; it does not set Human Visibility,
Tasks Ready, baseline, promotion or delivery.
