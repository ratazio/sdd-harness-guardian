# Evidence — T-002

**Status:** approved / task done. **Builder:** `spec021_t002_builder`.
**Evaluator:** `/root/evaluate_spec021_t002` (independent). **Approved:**
2026-08-30.

## Delivered increment

- Added the two source corpora under
  `scripts/fixtures/material-relation-composition/`: regulated clearing is a
  trust-boundary admission relationship, while reservoir operations is a
  failure/recovery progression. Neither scenario is selected by a domain
  taxonomy or fixed visual form.
- Each case carries request, canonical source, candidate HTML, rendered HTML
  fixture with source locator/provenance, and a distinct human review record.
  The reviewers explain why the boundary handoff table or recovery progression
  follows that corpus and cite why the other representation is N/A.
- Each corpus also has a structurally plausible negative candidate and a
  `REVISE` record that identifies the lost relationship, impossible decision,
  impact, canonical repair, rerender, and re-review owner.
- Added `scripts/test_material_relation_composition.py`. It only preserves
  declared record inputs, locators, source provenance and the intentional
  positive/negative recovery contrast; it contains no materiality detector,
  domain classifier, representation selector, visual quota, score, or
  semantic approval.

## AC / validation mapping

| Acceptance / validation | Builder proof |
|---|---|
| AC-021-03 / V-021-03 | The clearing reviewer approves a source-backed trust-boundary handoff; the reservoir reviewer approves a source-backed recovery progression. Both reviewers document a case-specific N/A for the other form. |
| Negative recovery proof | Both `review-lost.md` records are `REVISE` and make a real operational decision impossible until sources are recovered, rerendered and independently re-reviewed. |

## Commands run by the builder

```text
python scripts/test_material_relation_composition.py
python scripts/test_conditional_source_hook.py
python scripts/test_semantic_brief_review_calibration.py
python scripts/validate_bundle.py
```

## Independent evaluation

`/root/evaluate_spec021_t002` returned **APPROVE** on 2026-08-30. The
evaluation confirmed that the clearing corpus recovers its source-backed
trust-boundary admission relationship, the reservoir corpus recovers its
source-backed failure/recovery progression, and each representation is
justified by that specific corpus rather than by a template, quota, or domain
taxonomy. It also confirmed that the two negative candidates lose a material
decision and remain blocking `REVISE` records with repair/rerender/re-review
actions. The test stays within identity, locator, digest, provenance, and
positive/negative fixture wiring; it does not decide materiality, select a
representation, or grant semantic approval.

This approval closes T-002 only. It does not render SPEC 021, change T-003/T-004,
or set Human Visibility, Tasks Ready, baseline, promotion or delivery.
