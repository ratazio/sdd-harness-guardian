# Evidence Draft — T-003

**Status:** approved / task done. **Builder:** `spec021_t003_builder`.
**Evaluator:** `/root/evaluate_spec021_t003` (independent). **Approved:**
2026-08-30.

## Delivered increment

- Added `scripts/validate_semantic_review_record.py`, a bounded integrity
  validator for the optional `semantic-review-integrity/v1` envelope. It
  verifies only declared identity separation, record scope, candidate/rendered
  artifact bindings, and reviewer-declared corpus path/locator/SHA-256 entries.
- Added approved and revised human-record fixtures plus
  `scripts/test_semantic_review_integrity_contract.py`. Both human outcomes
  may have valid wiring; integrity does not silently elevate `APPROVE` into a
  semantic or delivery decision.
- Negative cases refuse same builder/reviewer identity, wrong candidate digest,
  missing corpus locator, wrong record scope, escaped manifest path, and an
  undeclared automatic-approval field.
- Documented the optional envelope in the corpus semantic review template.

## AC / validation mapping

| Acceptance / validation | Builder proof |
|---|---|
| AC-021-04 / V-021-04 | The focused validator accepts only a distinct reviewer record with exact candidate, rendered, corpus path/locator/digest, and explicit record scope. Negative fixtures prove integrity, identity, scope, and unknown-field violations are refused. |
| Human-decision boundary | The same integrity check accepts a human `REVISE` record and never makes an approval transition. The source has no score, classifier, materiality inference, visual/textual counter, or semantic-approval function. |

## Commands run by the builder

```text
python scripts/test_semantic_review_integrity_contract.py
python scripts/test_conditional_source_hook.py
python scripts/test_material_relation_composition.py
python scripts/test_semantic_brief_review_calibration.py
python scripts/validate_bundle.py
```

## Boundary preserved

This is integrity plumbing, not a human-review substitute. It cannot establish
whether the declared corpus is sufficient, whether any relation is material,
whether the rendered presentation is useful, or whether the reviewer’s verdict
should be accepted. Those remain independent human evaluation work.

## Independent evaluation

`/root/evaluate_spec021_t003` returned **APPROVE** on 2026-08-30. The
evaluation confirmed that the deterministic envelope accepts only a
reviewer-declared record with a distinct identity, exact candidate/rendered
scope and digests, and a corpus manifest whose paths, locators and digests
bind real files. It confirmed the negative regressions refuse integrity,
identity and scope violations, while valid `APPROVE` and `REVISE` records are
both merely well-bound human records. No score, domain classification,
visual/text counter, materiality inference or automatic semantic approval was
introduced; the human reviewer remains decisive.

This approval closes T-003 only. It does not start T-004, alter its status,
render, or set Human Visibility, Tasks Ready, baseline, delivery, or any
initiative lifecycle gate.
