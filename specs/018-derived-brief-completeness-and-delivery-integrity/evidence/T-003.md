# Evidence: T-003 — Enforce lifecycle and completeness at delivery gates

**Builder:** root  
**Status:** approved  
**Date:** 2026-08-27

## Delivered increment

- `stakeholder_brief_errors(..., rendered=True)` now rejects a v2 shell with
  `data-brief-phase="scaffold"`.
- Human Visibility emits a specific scaffold-phase gate diagnostic.
- Regression and smoke tests expect the new signal alongside other structural
  errors, avoiding a weak single-error assertion.

The rule examines only lifecycle state. It does not demand a tab count,
diagram, architecture, technology, browser, data store or visual arrangement.

## Validation

| Command | Result |
|---|---|
| `python scripts/test_brief_delivery_integrity_reproduction.py` | PASS — labelled scaffold rejected as rendered. |
| `python scripts/test_brief_composition_contract.py` | PASS |
| `python scripts/smoke_test_scaffolder.py` | PASS |
| `python scripts/validate_bundle.py` | PASS — 269 checks |

## Residual risk

The rule prevents false classification of the canonical scaffold; T-004 must
still demonstrate fresh authored briefs across unrelated domains and judge
their rendered decision usefulness independently.

## Independent evaluation

**Evaluator:** audit_018_root_cause (independent)  
**Decision:** approved — D-018-T003; expanded Human Visibility, Pearson and
client-identity regressions also passed.
