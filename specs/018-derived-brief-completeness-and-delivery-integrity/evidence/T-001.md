# Evidence: T-001 — Reproduce and inventory the empty-brief bypass

**Builder:** root  
**Status:** approved  
**Date:** 2026-08-27

## Delivered increment

Added `scripts/test_brief_delivery_integrity_reproduction.py`, an isolated
temporary-consumer reproduction of the observed pre-fix chain:

1. `new_initiative.py` creates a v2/Pearson shell without an explicit
   `data-brief-phase="scaffold"` and with visible generic instruction text.
2. `stakeholder_brief_errors(..., rendered=True)` currently returns no error
   for that shell, demonstrating the false rendered classification used by the
   existing smoke test.
3. After material `T-901`/`AC-901` IDs are added to the canonical sources, the
   existing composition parity check detects missing projections. The scaffold
   path does not use that finding as a delivery gate.

The fixture documents present behavior; T-003 must invert it into the desired
delivery-integrity regression without claiming that this task fixed it.

## Validation executed

| Command | Result | What it proves |
|---|---|---|
| `python scripts/test_brief_delivery_integrity_reproduction.py` | PASS | Three causal links reproduced in a disposable consumer. |
| `python scripts/test_brief_composition_contract.py` | PASS | Existing parity fixture still identifies rich source/brief divergence. |
| `python scripts/smoke_test_scaffolder.py` | PASS | Current scaffold and Pearson local-logo safeguards remain stable. |
| `python scripts/validate_bundle.py` | PASS — 269 checks | Bundle integrity unaffected by the reproduction fixture. |

## Acceptance and risk disposition

| Claim | Evidence | Result |
|---|---|---|
| AC-001/AC-003 failure mode is concrete before enforcement changes. | New isolated fixture plus current smoke path. | satisfied for discovery, not remediated. |
| Diagnostics can be based on artifact/category rather than source-body content. | Fixture asserts only phase/instruction/classification/projection conditions. | satisfied for discovery. |
| T-001 does not introduce a fixed domain/layout contract. | Fixture uses arbitrary IDs and no technology/layout assertion. | satisfied. |

## Residual risk / next step

The bypass remains intentionally present. T-002 must define an additive,
domain-neutral lifecycle/coverage contract; T-003 is the first task authorized
to change scaffold/validator enforcement. Independent evaluator must verify the
fixture is a faithful reproduction and not a self-fulfilling test.

## Independent evaluation

**Evaluator:** audit_018_root_cause (independent)  
**Decision:** approved — D-018-T001. The evaluator reran all four commands,
confirmed the arbitrary sentinels, and accepted the stated limitation that
this discovery fixture does not prove the future gate enforcement.
