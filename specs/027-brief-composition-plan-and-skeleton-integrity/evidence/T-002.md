# Evidence — T-002 skeleton contract and inheritance guard

**Builder:** `/root/spec027_skeleton_integrity`  
**Status:** implementation complete; independent evaluation pending  
**Scope:** FR-027-04..07; V-027-03, V-027-04 and structural portion of V-027-07.

## Delivered contract

- The vendor-neutral template now declares a shell contract. Its base
  stylesheet and navigation behavior are marked; existing `.slot` regions plus
  explicit header, metadata and document-title slots are the mutable regions.
- The inheritance guard verifies exact local path/hash and compares immutable
  DOM structure plus marked CSS/JS blocks. It reads no Markdown, generates no
  HTML and makes no visual or editorial choice.
- The regression materializes a positive in-situ copy and a negative parallel
  page. The negative preserves base hash, route IDs and navigation class, but
  fails for absent stylesheet, behavior and immutable shell.

## Commands and results

```text
python scripts/test_brief_candidate_inheritance.py  # PASS
python -m py_compile scripts/validate_brief_candidate_inheritance.py scripts/test_brief_candidate_inheritance.py  # PASS
python scripts/test_instantiate_brief_skeleton.py  # PASS
```

## Remaining gate

No renderer, Markdown parser, final HTML or mock run was changed. T-002 needs
evaluation by an identity distinct from this builder.
