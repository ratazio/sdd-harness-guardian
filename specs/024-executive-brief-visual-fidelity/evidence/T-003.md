# Evidence pack — T-003: aplicação visual heterogênea

**Task status:** done  
**Builder:** `/root/spec024_visual_heterogeneous_builder`  
**Evaluator:** `/root/spec024_visual_heterogeneous_reviewer`  
**Decision:** APPROVE, 2026-08-31

## Delivered, deliberately non-promoted corpus

Fresh, disposable root:
`testes/visual-reference-runs/20260831-spec024-heterogeneous/`.
It contains one offline Pearson HTML for each source request, each with eight
full query subpages (`?view=overview|scope|architecture|impact|execution|
validation|evolution|trust`) and a source/decision/non-adoption notice:

| Case | Architecture treatment | Domain-specific material shown |
|---|---|---|
| M-001 | material SVG | Next/App Router, session/admin boundary, Prisma/PostgreSQL and publishing flow |
| M-002 | material SVG | SFTP/CSV, immutable file custody, RabbitMQ worker, discrepancy and DLQ |
| M-003 | material SVG | Android local state, encrypted Room, WorkManager and recovery/sync states |
| M-004 | material SVG | RN/RN Web runtimes, shared domain, adapters and GraphQL boundary |
| M-005 | material SVG | local model, policy/human boundary and destination API (no direct MySQL) |
| M-006 | material SVG | quarantine, MinIO/jobs, local OCR/agents, deterministic validation and report |
| M-007 | `not-material` | accessible kiosk journey/cache/catalog/print; no fictitious backend SVG |
| M-008 | material SVG | Kafka, projection/watermark, incident and bounded reprocessing |

For material routes, the authored topology carries named nodes and relations,
all four legend states, a unit-bearing surface map and a supported factual
zoom. No file counts, SLAs, capacity or unsupported frontend details were
asserted. M-007 declares why software architecture is not material and shows a
source-backed operational journey instead.

## Rendered evidence and deterministic results

Each case has the following under `evidence/<case>/`:

- `architecture-1440x960.png`
- `architecture-390x844.png`
- `architecture-print.pdf`

`evidence/render-checks.json` records 8/8 cases with 8/8 routes showing one
main region and one active navigation item at desktop, no desktop/mobile
overflow, and eight routes exposed linearly with JavaScript disabled.

| Command | Result |
|---|---|
| `python scripts/build_spec024_heterogeneous_references.py` | pass — regenerated eight isolated HTMLs |
| `python testes/visual-reference-runs/20260831-spec024-heterogeneous/render_evidence.py` | pass — 16 PNG viewport captures plus 8 PDFs |
| `python scripts/test_architecture_visual_contract.py` | pass |
| local `architecture_visual_errors()` against eight HTMLs | pass — empty errors for M-001…M-008 |
| `python scripts/validate_bundle.py` | pass — 272 checks |

## Builder visual examination

The eight 1440×960 architecture captures were actually inspected. They show
the Pearson navy header/logo, lavender canvas, rounded white panels, strong
editorial hero and visible domain-specific headline/decision statement before
the diagrams. The previous aqua circle crossed the decision-card text in the
first render; it was placed behind the card, all eight references were
regenerated and captured again. The macro topology, surface map and zoom occur
below the first fold rather than being replaced by a linear text chain.

Residual limits by case are intentional rather than hidden: M-001 sandbox
facts remain fictional; M-002 excludes real acquirers; M-003 has no new
backend; M-004 does not promise runtime parity; M-005 does not claim model
quality or a frontend; M-006 excludes external finance actions; M-007 does not
invent a backend; M-008 does not promise real-time or distributed transaction
semantics. A distinct evaluator must still decide whether the rendered depth,
composition and source interpretation are adequate; structural green is not an
approval.

## Preservation

No path under `testes/mock-runs/20260831-spec023-t004-r1` was changed. At the
post-build checkpoint the historical M-005 candidate SHA-256 was
`40DCA6FE4F101C379FB3DD1103CC35CAAEF9D52475AE6AADDD420A08FF63C1A5`.
The new root's README explicitly says it neither promotes nor replaces T-004,
SPEC 023 or canonical sources.

## Next safe action

Assign an evaluator distinct from the builder to inspect the exact eight PNGs,
PDFs, HTMLs, sources and preservation marker. It may return REVISE; no
historical corpus is to be promoted from this evidence.

## Revision after independent REVISE

The evaluator correctly found that the first `architecture-1440x960.png`
captures proved the hero but not a usable visual architecture: the macro
topology fell below the fold; the mobile captures had the same issue; print
used a light inherited lede over white; and the first authored SVGs read as a
repeated five-box pipeline rather than distinct domain diagrams.

The repaired render now puts a compact, labelled architecture reading directly
inside the architecture hero, while retaining a full SVG topology below it.
The architecture hero is materially shorter on desktop and its mobile title /
lede scale is intentionally compact, so the graphic appears before the fold
for M-001, M-005, M-007 and M-008. Print overrides the lede to navy on white,
removes the decorative mini-graphic and keeps hero/diagram/panels together to
avoid a fragmented first page.

The macro diagrams were recomposed per domain: M-001 follows Next/App
Router/session/Prisma; M-002 adds immutable CSV custody and a DLQ/recovery
branch; M-003 staggers local state and sync/recovery; M-004 separates
runtimes/domain/adapters; M-005 now exposes FastAPI, worker, operational
PostgreSQL, Redis, local model, human policy and destination API; M-006 places
deterministic validation/review after OCR/agencies; M-008 exposes
projection/watermark and snapshot recovery. M-007 remains non-material and
labels its existing public catalog API as a preserved boundary.

After the repair, all eight HTMLs and 24 render artefacts were regenerated:
`python testes/visual-reference-runs/20260831-spec024-heterogeneous/render_evidence.py`;
`python scripts/test_architecture_visual_contract.py` and
`python scripts/validate_bundle.py` again passed (272 bundle checks). The
builder visually re-inspected desktop M-001/M-005/M-007/M-008 and their mobile
counterparts. This is evidence of repair, not a self-approval; T-003 remains
`needs_evaluation`.

## Second revision after independent REVISE

The second evaluator found two valid residual issues. First, M-005 print page
2 was an orphaned `Limite` strip followed by unused space. The print stylesheet
now suppresses repeated screen-only route footers (the route's limit remains
in its adjacent decision/scope content), avoiding a detached fragment. The
M-005 PDF was regenerated and rendered to PNG with Poppler for direct
inspection of pages 1–3: page 1 ends with the overview cards, page 2 begins
the next complete route, and page 3 contains the architecture topology rather
than an orphan limit. No white/low-contrast lede reappeared.

Second, the mobile hero's previous SVG labels were perceptible but too small.
Desktop retains the connected graphic; at 390px the same reading now becomes
three high-contrast labelled boundary chips (origin, material middle,
destination/recovery), with 11px text and no horizontal overflow. The builder
visually inspected the updated M-001, M-005, M-007 and M-008 mobile captures:
the labels are readable and the decision remains explicit beneath them.

Commands re-run: `python scripts/build_spec024_heterogeneous_references.py`,
`python testes/visual-reference-runs/20260831-spec024-heterogeneous/render_evidence.py`,
and the focused `.../render_evidence.py m007-kiosk m008-event-inventory` after
the responsive chip repair; `python scripts/test_architecture_visual_contract.py`
and `python scripts/validate_bundle.py` again passed (272 checks). This task
remained `needs_evaluation` until the distinct final review below.

## Independent evaluation — APPROVE

The distinct evaluator reviewed every desktop capture, selected mobile captures
(M-001, M-005, M-007 and M-008), M-005 print pages 1–3, the HTMLs, source
requests, render checks and preservation marker. It confirmed hero graphics are
visible before the fold, macro topologies are heterogeneous/factual, print no
longer orphans `Limite`, and the three mobile boundary chips are readable
without overflow. It reconfirmed local Pearson identity, query/no-JS behavior,
no T-004 mutation and M-007's reasoned `not-material` journey. T-003 is
approved as a non-promoted visual test corpus; no historical brief or T-004
candidate is thereby adopted.
