# Evidence: T-002 — canonical Pearson shell and default creation routes

**Task state:** `needs_evaluation` — revised after D-017; do not mark `done`.  
**Builder:** `build_t001_014`  
**Evaluator:** unassigned — must be distinct from the builder  
**Revision executed:** 2026-08-27  
**Decision:** D-018 submitted; D-013 remains the earlier approval, not approval
of this material asset-distribution revision.

## Revision outcome

D-017 truthfully found that an actual fresh consumer had the canonical relative
image URL but not its target file. The narrow repair preserves the URL,
`../../.harness/assets/brand/pearson-logo-white.png`, and avoids every
disallowed substitute (no hotlink, data URI or `vendor/` path):

- `scripts/new_initiative.py` defines the release SHA-256
  `8EEE1FA799766BF385A307191D38C361677D442457D7CC0F92E5F3FCCC2282F7`.
  It validates the bundle source and the consumer destination before creating
  scaffold state, then copies the source to the consumer’s
  `.harness/assets/brand/pearson-logo-white.png` only when it is absent.
- An existing consumer file with the exact hash is retained. A non-file target
  or a different hash fails with an explicit diagnostic and is never replaced.
- The existing canonical base, literal `pearson` profile, native named logo
  anchor, no-filter/no-network font boundary and v2 tab/provenance/no-script/
  print/reduced-motion contracts are unchanged.
- `smoke_test_scaffolder.py` now proves an actual temporary consumer has the
  required local file, exact hash and PNG dimensions 175 × 53. Its negative
  case supplies divergent bytes, asserts the clear failure, confirms those
  bytes are untouched and confirms no `specs/` scaffold state was created.
- `test_client_identity_profile_render.py` now scaffolds and serves a temporary
  **consumer** rather than the bundle source. The generated brief requests the
  local relative logo from that consumer’s same origin and the response status
  is asserted as HTTP 200, while retaining the responsive, keyboard, 200% zoom,
  reduced-motion, no-script and print checks.
- Template plan/README/manual-authoring guidance now disclose the provisioned
  local asset and manual-copy/no-substitution boundary.

## Validation

| Command / control | Result |
|---|---|
| `python -m py_compile scripts\new_initiative.py scripts\smoke_test_scaffolder.py scripts\test_client_identity_profile_render.py` | pass |
| `python scripts\test_client_identity_profile_contract.py` | pass — canonical Pearson default, local logo, no hotlink/filter |
| `python scripts\smoke_test_scaffolder.py` | pass — fresh feature/bugfix consumers receive the exact local logo; divergent pre-existing bytes cause `Scaffolding refused before target creation` and remain unchanged |
| `python scripts\test_client_identity_profile_render.py` | pass — serves a generated fresh consumer; logo is same-origin and HTTP 200; 320/390/768/1024/1440px, keyboard, 200% zoom, no-script, print and reduced motion pass |
| `python scripts\test_pearson_brief_policy.py` | pass — source-policy fixtures remain enforced |
| `python scripts\test_validate_human_visibility.py` | pass |
| `python scripts\test_tabbed_brief_surface.py` | pass |
| `python scripts\validate_bundle.py` | pass — 269 checks |
| `git diff --check` | pass |

The browser test’s asset observation is a direct HTTP response assertion, not
a screenshot inference. It is still builder evidence; final rendered review
and baseline work remain T-004 after distinct approval.

## Acceptance mapping

| Requirement / criterion | Builder proof | Result |
|---|---|---|
| FR-001 / AC-001 | Fresh scaffold retains literal `data-client-identity-profile="pearson"`; browser requests only the temporary consumer origin. | pass; revision evaluation pending |
| FR-002 / AC-003 | Canonical unconditional Pearson shell and fallback boundary remain covered by contract/policy checks; revision does not introduce an overlay or remote resource. | pass; revision evaluation pending |
| FR-003 / AC-002 | Actual consumer destination exists, SHA-256 matches the official asset, dimensions are 175 × 53 and generated-page request receives HTTP 200. Named native anchor/no role/filter/hotlink remains contract-tested. | pass; revision evaluation pending |
| FR-004 / AC-003/004 | Generated-consumer browser control retains keyboard/no-script/print/reduced-motion/responsive/zoom checks; human-visibility and tab-surface suites pass. | pass; T-004 still owns final independent rendered review/baseline |
| FR-005 | Guidance states explicit local-asset provision and a no-substitution boundary; no historical brief is rewritten. | pass; revision evaluation pending |

## Scope proof and evaluator checklist

This revision changes only the canonical scaffolder, its directly related
tests and authoring guidance. It does not alter existing brief layouts,
template identity, validators or T-003’s policy fixtures.

An evaluator distinct from `build_t001_014` must:

1. Re-run the listed focused controls and inspect the `new_initiative.py`
   preflight/copy order.
2. Independently create a temporary consumer; resolve the emitted image URL;
   verify file existence, exact SHA-256 and 175 × 53 dimensions; serve it and
   verify the generated page receives the local image successfully.
3. Seed a consumer-owned divergent destination and verify it fails clearly
   before target creation, does not overwrite the bytes and does not silently
   fall back to a remote/data/vendor path.
4. Confirm the root profile, named anchor, no filter/hotlink/remote font and
   flexible v2 semantic/provenance/tab/no-script/print contracts remain intact.
5. Record a separate approve/revise decision before setting T-002 `done`; only
   an approval may unblock T-004’s fresh-consumer rendered review and baseline.
