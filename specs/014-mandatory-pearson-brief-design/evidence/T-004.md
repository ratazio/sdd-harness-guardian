# Evidence: T-004 — rendered review and migration reconciliation

**Task state:** `needs_evaluation` — revised builder submission; do not mark
`done` and do not write a baseline.  
**Builder/reviewer:** `build_t001_014`  
**Evaluator:** unassigned — must be distinct from the builder  
**Executed:** 2026-08-27  
**Dependency:** D-019 independently approved T-002’s fresh-consumer asset
distribution repair.

## Rendered fresh-consumer approach

The review did **not** render the bundle template directly. It invoked
`new_initiative.py` into a temporary consumer, then served that consumer with
a local HTTP server and loaded
`/specs/001-render-control/stakeholder-brief.html` in headless Chromium.
`scripts/test_client_identity_profile_render.py --evidence-dir
specs/014-mandatory-pearson-brief-design/evidence/T-004-render` created the
attached evidence:

| Artifact | Actual evidence |
|---|---|
| `viewport-320x700.png`, `viewport-768x900.png`, `viewport-1024x900.png`, `viewport-1440x960.png` | Fresh-consumer responsive screenshots. `viewport-390x844.png` is retained as the supplemental narrow-width control. |
| `keyboard-tab-execution.png` | Focus begins on `#tab-impact`; ArrowRight activates visible `#tab-execution`. |
| `zoom-200-1440.png` | 200% zoom control; root and body scroll widths remain 1440px. |
| `reduced-motion-1440.png` | Chromium emulates `prefers-reduced-motion: reduce`; CSS media query matches. |
| `no-script-390x844.png` | JavaScript-disabled fresh-consumer capture; all eight tab panels remain available in source order. |
| `pearson-default-print.pdf` | Fresh-consumer print output with backgrounds; 260,878 bytes. |
| `render-review.json` | Browser route, same-origin request result, viewport metrics and behavioral outcomes. |
| `viewport-contact-sheet.png` | Reviewer contact sheet assembled from the four required viewport captures for direct comparison. |

`render-review.json` records `logo_statuses: [200]` and
`request_origins_same_as_consumer: true`. The contract/policy controls also
confirm no hotlink, data URI, CSS filter or remote font request. The only logo
asset is the provisioned local PNG with approved 175 × 53 intrinsic dimensions.

## Results by required review context

| Context / oracle | Result |
|---|---|
| 320 / 768 / 1024 / 1440px | No root or body overflow; screenshots show a navy logo band, lavanda canvas, white bordered decision cards and stacked narrow layout. |
| Keyboard | PASS — ArrowRight from the focused Impact tab selects and reveals Execution; the focus-visible rule remains present. |
| 200% zoom | PASS — no root/body horizontal overflow at 1440px. |
| Reduced motion | PASS — Playwright media emulation matched `reduce`; policy/contract checks retain the reduced-motion rule. |
| No script | PASS — eight panels are visible at 390px, in source order. |
| Print | PASS — generated PDF is non-empty and print CSS exposes panels. |
| Local asset / request | PASS — fresh generated page requests the official local PNG from same origin with HTTP 200. |
| Font authority | Bounded limitation — no local font binary or network request; D-005 retains declared system fallback until Plus Jakarta Sans packaging authority exists. |

## Guide comparison and revision

The screenshots have the guide’s dominant navy/lavender/white hierarchy,
high-contrast white logo on navy, restrained border-led cards, readable
spacing, visible focus treatment and responsive reflow. No visual inference was
used to change a migration classification.

The initial T-004 submission measured a 36.8px H1 and 166.391px logo at 320px.
This narrow revision changes only canonical responsive sizing and makes it a
fresh-consumer assertion: at 320px and 390px the logo must be 128–148px and
the H1 must be at least 40px. The recaptured metrics are **40px / 128px** at
both widths. At 768/1024/1440, logo widths are 144/144/172.797px, within the
guide’s desktop 144–176px range; the H1 is 40/40.96/57.6px. Intrinsic image
attributes remain 175 × 53 and `height:auto` preserves the logo proportion.

The evaluator must verify this narrow fix and the recaptured evidence rather
than treating the builder assertion as approval.

## Migration inventory reconciliation

The source inventory was rechecked against the stated corpus rule:

```text
INVENTORY_ROWS=9
CLASSIFICATIONS={'historical/legacy': 7, 'scheduled': 2}
OWNER_DATE_DECISION_REFERENCES=PASS
```

All nine included v2 paths (SPECs 006–014) have one permitted classification,
an owner, a non-empty decision reference that resolves in `decision-log.md`, a
target date and a source-backed rationale. Disposable `testes/mock-runs/**`
and `scripts/fixtures/**` remain exclusions. The two `scheduled` rows retain
their prior decisions/dates; the seven historical rows remain historical. No
row was promoted to `migrated`, and no exception was inferred from CSS or the
current screenshots.

## Commands

| Command | Result |
|---|---|
| `python -m py_compile scripts\test_client_identity_profile_render.py` | pass |
| `python scripts\test_client_identity_profile_render.py --evidence-dir specs\014-mandatory-pearson-brief-design\evidence\T-004-render` | pass — recaptured screenshots/PDF/JSON from a real fresh consumer; asserts 320/390 logo 128–148px and H1 ≥40px |
| `python scripts\test_client_identity_profile_contract.py` | pass |
| `python scripts\smoke_test_scaffolder.py` | pass — exact local asset and divergent-asset refusal |
| `python scripts\test_pearson_brief_policy.py` | pass |
| `python scripts\test_validate_human_visibility.py` | pass |
| `python scripts\test_tabbed_brief_surface.py` | pass |
| `python scripts\validate_bundle.py` | pass — 269 checks |
| inventory schema/classification/owner/date/decision control | pass — 9 rows, 7 historical/legacy, 2 scheduled |
| `git diff --check` | pass |
| `python scripts\validate_human_visibility.py --consumer-root . --initiative specs\014-mandatory-pearson-brief-design` | expected freshness failure only; baseline deliberately not written in this builder submission |

## Acceptance mapping and evaluator checklist

| Requirement / criterion | Builder evidence | Submission result |
|---|---|---|
| FR-003 / AC-002 | Fresh consumer receives same-origin local logo with HTTP 200; contract preserves native anchor/path/hash/dimensions/no filter. | pass; evaluator pending |
| FR-004 / AC-003/004 | Fresh-consumer screenshots/behavior cover responsive widths, keyboard, 200%, reduced motion, no script and print; 320/390 sizing assertions now enforce the guide-conformance revision. | pass; revised evaluator pending |
| FR-007 / AC-004 | Rendered evidence is a fresh consumer, not the source template. | pass; evaluator pending |
| FR-008 / AC-006 | Inventory has 9 reconciled rows with classification, owner, decision/date and rationale; no visual inference. | pass; evaluator pending |
| Human Visibility baseline | Intentionally withheld from builder phase. | pending evaluator and approved final decision |

The distinct evaluator must inspect the attached artifacts/JSON, recreate the
fresh-consumer route and all behavior controls, verify the 320/390 sizing
assertions and inventory dispositions without style inference, and only then
approve/revise. Baseline creation, baseline recheck and any `done` transition
are outside this submission.
