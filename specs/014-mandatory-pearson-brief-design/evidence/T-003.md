# Evidence: T-003 — deterministic prevention and diagnostic fixtures

**Task state:** `needs_evaluation`  
**Builder:** `build_t001_014`  
**Evaluator:** unassigned — must be distinct from the builder  
**Executed:** 2026-08-27

**Revision:** resubmitted after an independent evaluator reproduced three
bypasses: an unmarked material style injection, a second generic base and loss
of `brief-shell`/`brief-header` semantic hooks. This evidence supersedes the
initial T-003 submission recorded in D-014; it is still not approved.

## Delivered

- Added `scripts/validate_pearson_brief_policy.py`, a source-level contract
  validator linked from `scripts/validate_bundle.py`. It emits stable repair
  messages only; it never returns document body/source content in diagnostics.
- Added `scripts/fixtures/pearson-brief-policy/` and
  `scripts/test_pearson_brief_policy.py`. The fixture matrix proves:
  post-cutover missing/non-Pearson profile fails; remote, filtered and wrong
  logo use fails; an unnamed or `role="img"` logo anchor fails; a missing
  local guide fails; selector-overlay/generic reconstruction fails; an
  undocumented material override fails; and a dated historical/legacy
  exception plus a documented custom override remain valid.
- The validator requires an explicit exception only when a
  `data-harness-visual-override` style is declared. Its metadata records
  decision, owner, reason, retained decision/accessibility surfaces, review
  outcome and ISO re-review date. This does not infer a custom layout from
  arbitrary content.
- Updated canonical design guidance with the metadata grammar. The policy
  deliberately has no tab, section, card, diagram, content-domain or palette
  snapshot count; flexible per-tablist behavior remains under the SPEC 013
  contract.
- The canonical shell now declares exactly one
  `<style data-harness-pearson-shell>`. Any additional style must explicitly
  declare `data-harness-visual-override` and carry complete exception metadata.
  The policy also retains the stable `body.brief-shell` and `header.brief-header`
  hooks. These are source/provenance hooks, not a requirement for a particular
  task count, content layout or domain architecture.

## Validation

| Command / control | Result |
|---|---|
| `python -m py_compile scripts\\validate_bundle.py scripts\\validate_pearson_brief_policy.py scripts\\test_pearson_brief_policy.py scripts\\new_initiative.py scripts\\smoke_test_scaffolder.py` | pass |
| `python scripts\\test_pearson_brief_policy.py` | pass — all positive/negative fixtures, missing-guide control and no-body-leak sentinel |
| `python scripts\\validate_pearson_brief_policy.py` | pass — canonical source conforms |
| `python scripts\\test_client_identity_profile_contract.py` | pass |
| `python scripts\\smoke_test_scaffolder.py` | pass — fresh feature/bugfix output preserves Pearson default |
| `python scripts\\test_client_identity_profile_render.py` | pass — local logo, responsive, keyboard, no-script, zoom, print and reduced-motion contracts |
| `python scripts\\test_validate_human_visibility.py` | pass |
| `python scripts\\test_tabbed_brief_surface.py` | pass |
| `python scripts\\validate_bundle.py` | pass — 269 checks |
| SPEC 014 `validate_human_visibility.py` recheck | structural/state pass; expected freshness delta remains for independent T-004 rendered review/baseline refresh |

**Evaluator-revision controls:** `unmarked-material-style-injection.html`
fails the explicit-override contract; `generic-base-reconstruction.html` fails
the single canonical-base contract without comparing a palette snapshot; and
`missing-semantic-shell-hooks.html` fails both stable semantic hook checks.
`legacy-exception.html` and `documented-override.html` still pass.

## Acceptance mapping

| Requirement / criterion | Builder proof | Result |
|---|---|---|
| FR-006 / AC-001 | Missing/non-Pearson fixtures fail with a profile repair message; the dated historical/legacy control passes. | pass; independent evaluation pending |
| FR-006 / AC-002 | Remote/filtered/wrong logo and unsafe anchor fixtures fail; local path, 175×53, native named-anchor rules are checked. | pass; independent evaluation pending |
| FR-006 / AC-005 | Selector-overlay/generic reconstruction and undocumented override fixtures fail with actionable metadata/shell diagnostics. | pass; independent evaluation pending |
| FR-005 / FR-006 / AC-005 evaluator bypasses | Unmarked material CSS, a second declared base and body/header hook loss each have focused failing controls. | pass; independent evaluation pending |
| FR-004 / AC-003/004 | Existing human-visibility/tab/render suites pass; no fixed count was added to the policy. | pass; independent evaluation pending |
| FR-005 / AC-006 | Documented custom override and dated legacy exception controls pass; malformed legacy/override records fail. | pass; independent evaluation pending |

## Scope proof and evaluator checklist

T-003 changes only deterministic policy/checks, canonical exception guidance
and their isolated fixtures (plus the canonical style provenance attribute). It does not change the scaffolder, restyle
historical briefs, write the final Human Visibility baseline or perform T-004
rendered design review.

A distinct evaluator must independently run the focused fixture matrix and
bundle; inspect that each failure is scoped to a declared contract rather than
document content or a tab/section count; verify diagnostics omit the injected
body sentinel; and confirm the two permitted controls require complete,
dated evidence. Keep T-003 in `needs_evaluation` until that decision is
recorded. Do not start T-004 from this evidence alone.
