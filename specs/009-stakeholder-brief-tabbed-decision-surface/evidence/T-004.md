# Evidence: T-004 — rich task and validation projections

**Builder:** Codex / Builder-009  
**Evaluator:** distinct planner/reviewer — pending  
**Date:** 2026-08-26  
**Status:** proposed in D-023; do not treat as approval or as authorization for T-005.

## 1. Outcome and bounded change

T-004 projects existing task and validation sources into the accepted v2 tab
surface. It changes neither task/validation ownership nor the v1 contract. The
rich software candidate is the ignored news/blog sandbox; the paired
operational and negative sources remain the semantic calibration fixtures.

## 2. Source-to-projection mapping

| Canonical source | Rendered target | Recovered material fact | Absence behavior |
|---|---|---|---|
| `.harness/templates/tasks.md` | Execution guidance/template | Existing non-empty objective/outcome/increment, FR/AC/discovery, scope/anti-scope, contracts, dependencies, risk/assurance, validation/evidence, exit, state/authority and why-now fields. | No task title-only card when details exist; no invented file/action/priority. |
| `.harness/templates/validation-plan.md` | Validation guidance/template | AC/claim, method, command **or** manual context, fixture/data when relevant, expected result/oracle, evidence, owner and skip/limitation/risk. | No command is required for non-software or manual proof. |
| `testes/news-blog-spec-sandbox/.../tasks.md` | Sandbox `#execution` | Five cards now expose scope, anti-scope, FR/AC, dependency, risk/A1/A2, commands, evidence, exit and next-safe-step rationale. | Pending tasks stay visibly non-authorizing. |
| `testes/news-blog-spec-sandbox/.../validation-plan.md` | Sandbox `#validation` | AC groups map to Node/Docker context, db-clean/api-client/fault-db/logger-spy fixture, oracle, evidence owner and performance/coverage/manual limits. | Distributed load/rate limiting is an explicit MVP exclusion with owner/risk, not a missing command. |
| `scripts/fixtures/semantic-brief-review/field-operations` | `projection-cases.md` case | Managed-workspace handoff and 14:05 reconciliation have a manual context, zero-unmatched oracle and evidence destination. | No fake API or CLI is projected. |
| `scripts/fixtures/semantic-brief-review/shallow-negative` | `projection-cases.md` case | The hold/notification/oracle/evidence loss is material despite architecture N/A. | It must return to source correction rather than receive a generic proof card. |

## 3. Rendered artifacts

- `.harness/templates/stakeholder-brief.html` retains one HTML, eight panels,
  DOM-local provenance, tab keyboard/fallback and new content-contract prompts.
- `specs/009-stakeholder-brief-tabbed-decision-surface/stakeholder-brief.html`
  is regenerated as a real tabbed planning brief. Its Execution view projects
  T-001–T-005 in detail and its Validation view records AC/proof context and
  no-command limits.
- `testes/news-blog-spec-sandbox/specs/001-news-blog-auth/stakeholder-brief.html`
  is regenerated as the rich daily calibration brief: five concrete cards and
  an AC → command/context/fixture → oracle → evidence matrix.
- `scripts/fixtures/tabbed-brief-surface/projection-cases.md` records the
  rich-software, manual-operational and sparse-negative comparison without a
  duplicate schema.

## 4. Behavior and accessibility observations

Both regenerated briefs were opened as local HTML in the in-app browser.

| Observation | News/blog sandbox | 009 planning brief |
|---|---|---|
| Progressive enhancement state | `active` | `active` |
| Tabs / panels | 8 / 8 | 8 / 8 |
| Click Execution, then ArrowRight | Validation selected; fragment `#validation`; 7 panels runtime-hidden | Same result; 7 panels runtime-hidden |
| Rich content recoverable | Execution includes objective/increment cards; Validation includes command, fixture, oracle and evidence. | Execution includes all five task contracts; Validation includes AC/proof rows and explicit no-command boundary. |
| No-script/print baseline | No `tabpanel` has static `hidden`; print CSS forces `.tab-panel[hidden]` visible. | Same static contract. |

This is behavior proof, not a screenshot-only claim. Narrow viewport and final
print/no-script review are repeated by the independent release task T-005.

## 5. Validation run

| Check | Result | Boundary |
|---|---|---|
| JavaScript syntax extraction for both rendered HTML files | pass | Inline progressive enhancement parses. |
| `python scripts/validate_human_visibility.py --consumer-root testes/news-blog-spec-sandbox --initiative specs/001-news-blog-auth` | Structural/gate pass; freshness requests independent review and baseline refresh. | Expected: the rendered sandbox changed. |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/009-stakeholder-brief-tabbed-decision-surface` | Structural/gate pass; baseline is absent pending review. | Expected: baseline must be written only after review. |
| `python scripts/test_semantic_brief_review_calibration.py` | pass | Fixture wiring; not semantic approval. |
| `python scripts/test_brief_v2_contracts.py` | pass | Existing v1/v2 fixtures remain compatible. |
| `python scripts/test_validate_human_visibility.py` | `RESULT: PASS` | Validator fixture suite passes. |
| `python scripts/validate_bundle.py` | `Bundle validation passed: 267 checks.` | Bundle structure/reference integrity. |

## 6. Proposed U-004 resolution and residual risk

The proposed no-command contract is: retain the AC/claim, method, manual
environment/context, relevant data/fixture, oracle, evidence destination,
owner and skip/limitation with residual risk. A CLI command is optional. This
is shown by the field-operations reconciliation and by the explicit bundle
boundary; it remains pending distinct evaluation as D-023.

Residual risk IR-002/IR-003: a card or row could drift from source, or a
source-backed absence could be made to look like proof. The evaluator must
trace one rich task and one AC row to source, verify the manual/no-command case,
reject invented facts, and confirm pending status never authorizes a task.

## 7. Independent-evaluation ask

Verify source fidelity for the rich sandbox and 009 cards/matrix, child-level
provenance, honest no-command/N/A behavior, visible authorization state and
the live tab isolation evidence. Approve only if D-023 resolves U-004 as
guidance and leaves T-005 separately gated.

## 8. D-024 state-reconciliation revision

The distinct reviewer returned D-023 with one material state mismatch: the
009 derived brief still said T-004 was `in_progress` under D-022, although
the canonical task ledger, run state and progress had already moved it to
`needs_evaluation` under D-023. This is a source/authority defect, not a
request to weaken the rich-card or no-command contract.

The correction first reconciled `progress.md`, then regenerated the complete
009 brief from the canonical D-023 state. The regenerated header, snapshot,
Execution callout/card, Evolution checkpoint, Decision callout and Coverage
locator now state that T-004 evidence awaits distinct evaluation and that
T-005 remains pending. D-024 supersedes D-023 as the proposal being reviewed.

The next evaluator must recheck that state against `run-state.yaml`,
`tasks.md`, `progress.md` and `decision-log.md#D-024`, as well as the original
source-to-card and no-command checks. This correction is not approval and it
does not permit baseline generation or T-005.

Repeated after regeneration: `python scripts/test_semantic_brief_review_calibration.py`,
`python scripts/test_brief_v2_contracts.py`,
`python scripts/test_validate_human_visibility.py` and
`python scripts/validate_bundle.py` passed (267 bundle checks); `git diff
--check` was clean except the pre-existing CRLF notice for `specs/INDEX.md`.
The 009 Human Visibility command has no structural/gate failure and only the
expected missing freshness baseline. In the in-app browser, the regenerated
brief started at `#execution` with enhancement `active`, seven runtime-hidden
panels, visible `needs_evaluation` and T-005 pending text; ArrowRight selected
Validation, set `#validation` and retained seven runtime-hidden panels.
