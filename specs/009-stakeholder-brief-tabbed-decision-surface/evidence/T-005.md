# Evidence: T-005 — compatibility, fixtures and release readiness

**Task:** T-005 — Validate compatibility, fixtures and release evidence independently  
**Builder:** Codex / Builder-009  
**Evaluator requested:** distinct maintainer / `/root/sandbox_coverage_review`  
**Status:** proposed in D-027; no release or baseline is authorized by this file.

## 1. Focused deterministic protection

T-005 found a bounded structural gap: the v2 lifecycle and Human Visibility
tests protect provenance/gates, but did not directly protect the stable tab
surface that makes the one-document view usable. The new
`scripts/test_tabbed_brief_surface.py` tests only the canonical v2 template
and its focused reference fixture for:

- exactly eight ordered tab IDs and matching `aria-controls`/fragment targets;
- exactly eight resolvable panels, none statically `hidden`;
- one tablist, the pre-enhancement fallback marker, inline/offline script and
  the print rule that reveals runtime-hidden panels; and
- the stable progressive interaction tokens for arrows and print restoration.

It does not inspect prose quality, source adequacy, business semantics,
diagram complexity, a score, a parser model or an LLM judgment. Richness and
proportionality remain the distinct reviewer’s responsibility.

## 2. Command matrix

| Command | Result | Oracle / evidence boundary |
|---|---|---|
| `python scripts/test_tabbed_brief_surface.py` | pass | Template and v2 fixture preserve the eight-tab/fallback/print surface. |
| `python scripts/test_brief_v2_contracts.py` | pass | Legacy v1 and v2 lifecycle/coverage contracts remain compatible. |
| `python scripts/test_validate_human_visibility.py` | `RESULT: PASS` | Validator fixture suite remains valid. |
| `python scripts/test_semantic_brief_review_calibration.py` | pass | Fixture calibration/wiring only; explicitly not semantic approval. |
| `python scripts/validate_bundle.py` | `Bundle validation passed: 267 checks.` | Bundle structure and references remain valid. |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/009-stakeholder-brief-tabbed-decision-surface` | structural/gate pass; expected freshness baseline missing | 009 baseline must be created only after independent rendered review. |
| `python scripts/validate_human_visibility.py --consumer-root testes/news-blog-spec-sandbox --initiative specs/001-news-blog-auth` | structural/gate pass; expected baseline stale | Sandbox brief changed after prior baseline; refresh only after independent rendered review. |
| `git diff --check` | clean, except pre-existing CRLF notice for `specs/INDEX.md` | No whitespace error from this work. |

## 3. Final rendered/manual behavior prepared for review

The final 009 brief was regenerated to D-027 and opened as local HTML in the
in-app browser. At `#execution`, progressive enhancement was `active`, the
Execution panel was selected, eight panels existed and seven were
runtime-hidden. Its visible text showed T-004 done and T-005 as `needs
evaluation`. ArrowRight selected Validation, set `#validation`, retained seven
runtime-hidden panels, and the stylesheet exposed a print media rule. The
static source has no `hidden` panel. The sandbox received the same interaction
proof in T-004.

The reviewer must check the final source state, desktop/390px, keyboard,
no-script and print behavior for
the template/reference/sandbox as proportionate, not infer it from a
screenshot.

## 4. Compatibility, security and scope review

- v1 template/legacy path was not edited; only v2 canonical template,
  guidance, fixtures, validator-compatible provenance, test and the ignored
  sandbox brief changed.
- Search/diff review found no score, prose parser, semantic pass claim, LLM
  judge, remote asset, router, framework, API, consumer application code or
  duplicate canonical sidecar.
- The test reads only static repository assets. The sandbox remains explicitly
  fictitious; no credential, personal data or production topology is added.
- Residual risk: a real consumer can still supply thin source material. The
  design standard requires source-backed detail, concise N/A or an owned
  material question; the independent review, rather than this test, catches
  misuse of that policy.

## 5. Independent release-review ask

Review T-005 as a distinct maintainer: verify the focused test remains
structural, check final tab/fallback behavior and source authority across the
template/reference/009/sandbox, confirm v1 is untouched, and decide whether
the two affected Human Visibility baselines may be written. Approval must keep
release evidence separate from any automatic semantic score.

## 6. D-028 state-reconciliation revision

The distinct reviewer returned D-027 for one canonical-authority mismatch:
the D-027 brief already showed T-004 done and T-005 `needs_evaluation`, but
`progress.md` still said that this brief needed regeneration. The correction
first changes that canonical progress statement to “synchronized”, then
rechecks the complete derived brief against the unchanged D-027 task/state.

D-028 supersedes D-027 as the proposal under review. It changes neither the
structural-test scope nor the command results; it does not authorize a
baseline or release. The next reviewer must compare `progress.md`,
`run-state.yaml`, `tasks.md`, `decision-log.md#D-028` and the rendered brief,
then decide whether post-review baselines may be written.

After the D-028 regeneration/recheck, the local browser opened `#execution`
with eight panels, seven runtime-hidden panels and enhancement `active`; the
Execution, Decision and progress checkpoint all exposed D-028. ArrowRight
again selected Validation, set `#validation` and retained seven runtime-hidden
panels. This observation supplements, but does not replace, the evaluator’s
own rendered/accessibility judgment.

## 7. D-029 baseline-provenance correction

The distinct maintainer accepted D-028/T-005 review and authorized baseline
writing, but returned the first generated baselines because their metadata was
copied from the historical Human Visibility review records (D-012 for 009 and
D-009 for the sandbox). D-029 records the current independent review as the
canonical review record; its text also keeps the completed D-011 propagation
record explicit for the v2 lifecycle contract.

The D-029 brief state is regenerated before rerunning the baseline writer for
both briefs. The JSON files are never edited directly. After both fresh
validators pass with the D-029 record, only a separate maintainer release
decision may close T-005.

## 8. D-030 release-evidence acceptance and closing action

The distinct maintainer accepted the bundle release evidence in D-030 after
the D-029/D-010 writer-generated baselines passed. The acceptance covers the
bundle only: it does not authorize external deployment. It confirms that the
focused tab test remains structural-only and that independent rendered review
is not replaced by a score, parser or automated semantic claim.

The builder now refreshes both baselines once more through the validator after
the final D-030 source state, then runs the complete deterministic suite. That
mechanical refresh is evidence closure, not another review or decision.
