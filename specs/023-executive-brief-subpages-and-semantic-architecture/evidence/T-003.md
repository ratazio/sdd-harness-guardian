# Evidence Pack: T-003

**Initiative:** 023-executive-brief-subpages-and-semantic-architecture  
**Task:** T-003  
**Spec revision/link:** ../spec.md  
**Builder ID:** /root/spec023_t003_builder  
**Evaluator ID:** /root/spec023_t003_reevaluator  
**Started/finished:** 2026-08-31 / pending  
**Decision:** approve — independent evaluation confirmed source fidelity and executive depth.  
**Assurance profile / contract link:** A2-elevated; `tasks.md#T-003`.

## 1. Implementation summary

Added the source-grounded composition support that T-003 needs without making
the renderer, the shell or a canonical Markdown file decide editorial quality.
The authored route map describes the eight decision domains and pins every
material assertion to an existing source/locator, allowed synthesis, boundary
and derived-block locator. Three minimal heterogeneous projections make the
architecture contract inspectable: a source-supported product relationship, a
non-software operating recovery with the water-quality-lead authorization
preserved, and an explicitly unowned macro-only gap. Each opening now makes a
source-backed thesis, decision pillars, material limit and next conversation
explicit rather than leaving the executive reading implicit.

The existing SPEC 023 candidate is recorded only as a derived reference. Its
`run-state.yaml` provenance is stale after completed T-001/T-002 transitions,
so this task does not render, refresh or present it as current evidence. A
fresh candidate must be composed and reviewed in the later controlled corpus
step rather than patched around its lifecycle binding.

## 2. Files and contracts changed

| File/contract | Change | Why in scope |
|---|---|---|
| `evidence/T-003-editorial-map.md` | Eight-route editorial map, architecture-depth cases and independent-review question. | Additive composer handoff over canonical sources. |
| `scripts/fixtures/executive-brief-editorial-contract/*/candidate.html` | Minimal source-attributed architecture projections for material, operational and unknown cases. | Makes macro/change-map/scale/zoom or N/A/discovery inspectable. |
| `scripts/test_executive_brief_experience_composition.py` | Narrow fixture wiring for locators, dispositions and absence of fabricated owner/path. | Regression without a semantic score or visual quota. |

## 3. Acceptance and exit-criteria coverage

| AC/exit ID | Builder result | Validation ID | Evidence/output |
|---|---|---|---|
| AC-023-03 | pending independent evaluation | V-023-03 | Eight-route map gives a route-specific question, source, synthesis and limit. |
| AC-023-04 | pending independent evaluation | V-023-04 | `learning-release` material architecture; `reservoir-operations` proportional operational relation with unknown physical scale; `missing-internal-detail` discovery. |
| AC-023-05 | pending independent evaluation | V-023-05 | Scale names its unit/denominator when supported; macro-only fixture rejects false zero and unsupported owner/path. |
| T-003 exit: source-bound projections | pass structurally | V-023-03/04/05 | New fixture wiring check and existing T-001 editorial contract test. |
| T-003 exit: executive depth/visual proportionality | pending | V-023-03/04/05 | Reserved to a distinct experience evaluator. |

## 4. Validations executed

| Command/check | Result | What it proves / does not prove |
|---|---|---|
| `python scripts/test_executive_brief_experience_composition.py` | pass after REVISE repair | Binds fixture blocks to known source headings and dispositions only. |
| `python scripts/test_executive_brief_editorial_contract.py` | pass before this projection addition | Preserves source-supported discovery-owner bifurcation. |
| `python scripts/test_tabbed_brief_surface.py` | pass before this composition addition | Existing route mechanics; not a narrative verdict. |

## 5. Known gaps and requested review

| Gap/risk | Impact | Required next action |
|---|---|---|
| A structural fixture cannot decide whether the prose is ready for a C-level meeting. | Generic or shallow explanation could still pass. | Distinct evaluator applies `executive-brief-experience-review` to request, sources, map and projections. |
| The SPEC 023 candidate has a stale state digest after T-001/T-002. | It cannot be rendered or called a fresh output. | Recompose only through the lifecycle/provenance process; do not patch delivery HTML. |
| Fixture candidates focus on architecture depth rather than simulating a full production brief. | They calibrate the conditional architecture contract, not all route copy. | Reviewer checks the eight-route map and T-004 evaluates the full diverse corpus. |

## 6. Revision response

| Independent finding | Repair | Revalidation required |
|---|---|---|
| Reservoir macro/change map treated the second sample as sufficient and dropped the source-backed water-quality-lead authorization. | Restored the authorization in thesis, pillars, macro and preserved safety boundary. | Re-run the narrow fixture test and require the next evaluator to inspect the recovery decision. |
| Reservoir called its physical/operating scale N/A. | Replaced it with `unknown`: the exact needed unit is named physical assets or operating surfaces, and the source lacks denominator/inventory; no zero is claimed. | Re-run the narrow fixture test and require the next evaluator to judge proportionality. |
| The three openings did not explicitly expose thesis, pillars, material limit and next conversation. | Added these four source-grounded editorial elements to each heterogeneous candidate. | Distinct reviewer judges whether the openings are decision-ready, not the test. |

## 7. Builder handoff

**Implementation status:** `needs_evaluation`.  
**Requested evaluator:** a distinct `spec023-executive-composition-evaluator`
using `.harness/skills/executive-brief-experience-review/SKILL.md`; do not edit
files during review.  
**Review inputs:** requester intent; `spec.md`, `plan.md`, `impact-map.md`,
`tasks.md`, `validation-plan.md`, `ratchet.md`; this evidence pack and
`T-003-editorial-map.md`; the three candidate fixtures; and the stale-state
limitation of `tmp/spec023-brief-candidate.html`.

## 8. Independent evaluation

Pending. The evaluator must issue `APPROVE` or `REVISE` with request/source/map/
artifact locators, decision impact, canonical recovery and the originating
reviewer needed after repair. The builder must not approve this evidence.
