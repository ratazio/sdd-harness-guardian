# Evidence Pack: T-004

**Initiative:** 023-executive-brief-subpages-and-semantic-architecture  
**Task:** T-004  
**Spec revision/link:** ../spec.md  
**Builder ID:** /root/spec023_t004_builder  
**Evaluator ID:** /root/spec023_t004_clevel_reevaluator  
**Started/finished:** 2026-08-31 / 2026-08-31  
**Decision:** approve — independent five-lens review decided `preserve historical` for M-001–M-008; no candidate is adopted.  
**Assurance profile / contract link:** A2-elevated; `tasks.md#T-004`.

## 1. Increment delivered

Created the fresh disposable corpus
`testes/mock-runs/20260831-spec023-t004-r1/` from the complete M-001–M-008
planning packages. The prior generated packages are preserved as historical
laboratory inputs; the T-004 builder did not overwrite their
`stakeholder-brief.html`, alter their Markdown authority, create a baseline or
promote a refresh.

Each case now has a separate `t004-candidate.html` with the T-002 internal
route shell: eight complete decision panels addressed by `?view=`, route focus
and history behavior, no-script source order, print expansion and reduced
motion support. The candidate retains the source-attributed content that was
already present in the copied package, moves its executive snapshot into the
Scope route, keeps material architecture relations within Architecture and
conditional governance within Evolution, and adds only non-factual decision
orientation text. Its companion `t004-candidate-decision.md` records a hash,
pending adoption decision and historical-brief preservation boundary.

The corpus is deliberately **vendor-neutral**. Pearson is not inferred from a
mock request: the explicit, local Pearson shell is exercised by the canonical
identity/browser controls below. A reviewer must decide whether any particular
case has a source-backed reason to opt in before such a profile is selected.

## 2. Corpus and candidate decisions

| Mock | Fresh consumer root / initiative | Candidate SHA-256 (UTF-8) | Decision | Historical brief |
|---|---|---|---|---|
| M-001 | `.../m001-news/specs/001-news-blog-auth` | `e2eb47b6e8d25006fa78e575d47ffda0fc67b6447c49a7bf313c4dd89275b1fb` | pending five-lens review | preserved |
| M-002 | `.../m002-backend/specs/001-settlement-reconciliation` | `851abcda8c92fd597e5483b639bb0c957fc099dff55ad62cbf222228af624148` | pending five-lens review | preserved |
| M-003 | `.../m003-mobile/specs/001-offline-field-inspections` | `9ecebc1b67599aa4bf320c4fea367afaa3e28fc82734e590d8f1663524b475e8` | pending five-lens review | preserved |
| M-004 | `.../m004-rn/specs/001-learning-multiplatform` | `675b173d81476c755713367188857194905de676685624f99bb5814745b9cd67` | pending five-lens review | preserved |
| M-005 | `.../m005-ai/specs/001-local-ai-exam-scoring` | `764d85f7d76e98bb0af2a78400703a0af973ffb3d17eb890dffb347d26b6db49` | pending five-lens review | preserved |
| M-006 | `.../m006-agentic/specs/001-agentic-financial-document-reports` | `acb92e6ae4206293f48743a5053aff7a77d23782b3f0bb628e623de3b7c1d4f6` | pending five-lens review | preserved |
| M-007 | `.../m007-kiosk/specs/001-accessible-public-kiosk` | `6fd3f03595909f93c9a5546bc5778beb98f2620605b0556c0f690850e97d2e61` | pending five-lens review | preserved |
| M-008 | `.../m008-events/specs/001-event-driven-inventory-recovery` | `ebe85888ac87acd8cf1a05b656655853714f8a9cd7c0d0ec2cbd7913e0999521` | pending five-lens review | preserved |

Each manifest entry now records the raw request path and SHA-256, the current
copied-canonical-source manifest SHA-256, the candidate SHA-256 and its named
pending decision envelope. The complete roots and isolation statement are in
`testes/mock-runs/20260831-spec023-t004-r1/run-manifest.json`.

## 3. Acceptance and exit-criteria coverage

| AC / exit criterion | Builder result | Validation / evidence | Boundary |
|---|---|---|---|
| AC-023-01 / internal domains | pass structurally | Every candidate has exactly the eight required `tabpanel` routes and one `?view=<route>` tab for each. | Does not judge narrative quality. |
| AC-023-02 / accessible shell | pass structurally | Existing browser control confirms local focus/history, 320–1440px, no-script, print and reduced motion mechanics. | Control fixture, not a C-level verdict on all eight mocks. |
| AC-023-03–05 / source-backed executive explanation | repaired; pending qualitative review | Each candidate now replaces the generic four-box flow with its own source-backed requested topology/journey; M-004 has a supported monorepo/adapters cut. | Reviewer must check source → fact → block → decision case by case. |
| AC-023-06 / Pearson opt-in | pass control; pending corpus decision | Explicit local Pearson policy and browser test pass; candidates remain neutral absent source authorization. | A green policy test cannot authorize Pearson for a mock. |
| AC-023-07–08 / controlled adoption | pending | One explicit `pending independent five-lens review` decision per candidate; no baseline or historical mutation. | Evaluator must make an adopt / refresh / preserve / revise decision per artifact. |
| T-004 exit: M-001–M-008 proportionate evidence | builder pack complete; evaluation pending | Manifest, eight candidate decisions, hashes and control artifacts retained. | No self-approval. |

## 4. Validations executed

| Command / check | Result | What it proves / does not prove |
|---|---|---|
| T-004 candidate shell/composition static check (eight candidates) | PASS | Exactly eight route panels, query targets, no primary `scrollIntoView`, history/print hooks, snapshot nested in Scope and pending/non-adopted status. It does not rate prose. |
| `python scripts/test_tabbed_brief_surface.py` | PASS | Shared v2 route contract and negative route/snapshot regressions. |
| `python scripts/test_client_identity_profile_contract.py` | PASS | Neutral default and explicit/local Pearson boundary. |
| `python scripts/test_pearson_brief_policy.py` | PASS | Pearson policy fixtures and local identity rules. |
| `python scripts/test_client_identity_profile_render.py --evidence-dir testes/mock-runs/20260831-spec023-t004-r1/control-evidence` | PASS | Observable local control fixture at 320/390/768/1024/1440, print PDF, focus/history/no-script/reduced motion. |
| Actual-candidate browser/print review (M-001–M-008) | PASS | Every real candidate selected Architecture by `?view=`, showed one active route under JS, had no root overflow at 1440px or 320px, exposed eight routes without JS, and expanded eight panels for PDF print. Artefacts are per case in `candidate-evidence/`. |
| `python scripts/validate_bundle.py` | PASS — 272 checks | Bundle structural invariants only; it does not approve mock candidates. |

## 5. Retained visual control artifacts

`testes/mock-runs/20260831-spec023-t004-r1/control-evidence/` contains
`pearson-320x700.png`, `pearson-390x844.png`, `pearson-768x900.png`,
`pearson-1024x900.png`, `pearson-1440x960.png`, `pearson-print.pdf` and
`render-review.json`. These are evidence of the controlled shell only, not
screenshots offered as proof that the eight narratives meet the meeting bar.

`testes/mock-runs/20260831-spec023-t004-r1/candidate-evidence/` now contains,
for every real M-001–M-008 candidate, an Architecture screenshot at 1440px, a
Scope screenshot at 320px and a print PDF. `candidate-browser-review.json`
records the actual route, responsive, no-script and print observations.

## 5a. C-level REVISE response

| Finding | Repair retained in the candidate | Source boundary |
|---|---|---|
| Generic four-box/arrows did not explain the actual requested topology. | M-001 names Next.js/API/AuthZ/Prisma/PostgreSQL; M-002 follows file→queue→reconcile→divergence with non-destructive rollback; M-003 shows offline recovery states; M-004 names monorepo/mobile/web/adapters and has a supported adapter zoom. | Each panel retains plan provenance and a visible textual equivalent. |
| The remaining cases still needed decision-specific journeys. | M-005 separates source API/service/local model/destination API and deterministic versus probabilistic authority; M-006 shows quarantine before extraction/agents/rules/report and Docker boundaries; M-007 shows search/route/print/contingency; M-008 shows source-of-truth, watermark, active/shadow recovery and rollback. | No component, owner, scale or external system was added beyond the copied plan/request facts. |
| Candidate decision records were not complete enough for re-evaluation. | Each case now has a named pending envelope with builder/reviewer status, request path+SHA, canonical-source manifest SHA, exact candidate SHA, adoption boundary and next safe action. | The envelope remains pending and is outside the canonical source authority; it cannot promote or authorize delivery. |
| A real candidate exposed narrow-width overflow from the shared shell. | Narrow v2 header padding, child min-width and wrapping were corrected; all eight candidates were rechecked at 320px and 1440px. | This is an additive shell accessibility repair; no mock source or historical delivery artifact changed. |

## 6. Known limitations and requested independent review

The copied mock inputs came from the earlier `20260830-spec021-t004-r15`
laboratory corpus. They are diverse and complete planning packages, but their
historical delivery HTML retains its own lifecycle. T-004 candidates are new,
pending, derived inputs; they cannot inherit that old review, its render state
or its authority.

The evaluator must be distinct from this builder and use the
`executive-brief-experience-review` lens. For each M-001–M-008, review raw
request, copied canonical Markdown, `t004-candidate.html` and its decision
record on five lenses: **purpose**, **perimeter**, **trade-off**, **next
action** and **confidence**. The review must identify source and candidate
locators, distinguish deterministic evidence from judgment, and record exactly
one adoption outcome: `adopt`, `refresh`, `preserve historical`, or `revise`.
The reviewer must not edit candidates. A REVISE returns only the cited case(s)
to the builder; no broad historical migration is authorized by this pack.

## 7. Builder handoff

**Implementation status:** `needs_evaluation`.  
**Requested evaluator:** distinct `spec023-c-level-evaluator`, with no editing
authority, reviewing the fresh root and this evidence pack.  
**Next safe action:** issue per-artifact five-lens findings and decisions; keep
all historical briefs intact unless a later authorized migration/refresh is
explicitly chosen.

## 8. Independent evaluation

Pending. This builder must not fill this section or transition T-004 beyond
`needs_evaluation`.
