# Handoff: 023-executive-brief-subpages-and-semantic-architecture

**From:** Codex
**Intended role/recipient:** Delivery Orchestrator / coverage author
**Created at:** 2026-08-31
**Current phase/status:** T-003 independent evaluation pending / needs_evaluation
**Current task/status:** T-003 is implemented by `/root/spec023_t003_builder`; do not start T-004 until a distinct evaluator records the outcome.
**Last safe checkpoint:** source-locator map covers all eight decision domains; three heterogeneous architecture projections remain additive evidence and the stale SPEC candidate was not promoted.
**Repository revision/working-tree summary:** shared worktree is already dirty
from earlier Guardian work; SPEC 023 source package and visual mocks are
additive. Do not reset, discard or mass-format unrelated changes.

## 1. Completed and approved work

- SPEC 023 source package, design direction and M-023-A/B/C exist.
- Requester approved the direction in D-023-001, including a more vertical
  composition and conditional responsible-name treatment.

## 2. Partial or unverified work

- T-001 added isolated `executive-brief-composition` and
  `executive-brief-experience-review` skills plus matching isolated agents;
  existing Guardian roles were left intact.
- T-001 added source-grounded calibration fixtures for a material product
  architecture, non-software operations and missing internal detail, and an
  evidence draft. Their test is intentionally non-semantic; distinct evaluation
  remains the blocking task transition.
- T-002 changed only the additive reusable shell and test fixture: each view is
  a complete `?view=` internal route with a more vertical reading territory;
  no avatar/login, remote resource, Markdown source, real brief content or
  existing Guardian skill was changed. Its evidence and captures are awaiting
  the distinct shell evaluator.

- D-023-013 independently approved one frozen candidate for guarded rendering.
- D-023-014 independently confirmed internal-subpage mechanics, local Pearson
  identity and honest architecture depth, but returned the first render.
- D-023-015 confirms those three repairs but returns the recovered candidate
  for stale lifecycle/digest projections and malformed logo markup.
- D-023-017 confirms all substantive repairs, but returns the pending record
  because its composition manifest was calculated before the coverage map stabilized.
- D-023-019 confirms the promoted experience but returns stale pre-render
  wording and coverage rows that cannot be structurally resolved.
- D-023-021 confirms those coverage rows after correction but returns the
  decision digest and identifies a false positive in the Human Visibility validator.
- D-023-023 refutes that diagnosis: the checker is correct and the colon
  format is the contract; no production patch or exception is required.
- D-023-025 confirms the candidate substance but returns a static coverage
  gate and next-step sentence that cannot survive signature and refresh.
- D-023-027 confirms those lifecycle repairs but returns the next-step marker
  because it has no v2 `data-source` binding.
- D-023-029 confirms rendered structure and narrative but returns wording
  that would misdescribe the current post-render review.

## 3. Files changed

| File | State | Reason |
|---|---|---|
| `validation-plan.md` | corrected source | names the four real future evidence destinations |
| `decision-log.md`, `run-state.yaml`, `progress.md`, `plan.md` | recovery checkpoint | D-023-029 turns stale pre-render wording into historical/post-render communication |
| evidence/visual-mocks/ | context only | non-canonical decision images |

## 4. Validations and evidence

| Task/check | Result | Evidence |
|---|---|---|
| M-023-A/B/C visual review | requester approved | decision-log.md#D-023-001 |
| `validate_bundle.py` before D-023-014 | pass | 272 checks |
| Post-render review | REVISE | `decision-log.md#D-023-014` |
| Pre-render recovery review | REVISE | `decision-log.md#D-023-015` |
| Composition-binding review | REVISE | `decision-log.md#D-023-017` |
| Post-render review | REVISE | `decision-log.md#D-023-019` |
| Pre-render review | REVISE | `decision-log.md#D-023-021` |
| Validator audit | CORRECTION | `decision-log.md#D-023-023` |
| Pre-render review | REVISE | `decision-log.md#D-023-025` |
| Pre-render review | REVISE | `decision-log.md#D-023-027` |
| Post-render review | REVISE | `decision-log.md#D-023-029` |

## 5. Decisions and approvals

- D-023-001 authorizes execution but preserves every v2 lifecycle gate.
- D-023-013 applies only to the historical first candidate; it cannot approve
  a corrected candidate.
- D-023-014 requires a table-level coverage ID, real evidence destinations and
  `tasks.md — Contrato de autorização A2` as the source of execution authority.
- D-023-015 required current-state projections, digests and Pearson logo repair.
- D-023-017 requires a current composition-manifest binding; D-023-018 cannot
  become an approval before another independent review.
- D-023-019 requires rendered lifecycle projections and one-fact coverage rows
  before D-023-020 may be reviewed.
- D-023-023 confirms no validator repair is needed; D-023-029 requires the
  current post-render state to be communicated before D-023-030 may receive
  an exact review.
- `ready_to_render` / `render_pending` is a source checkpoint for the next
  candidate only. Coverage, Human Visibility and Tasks Ready are false.

## 6. Blockers, unknowns and risks

- Corrected candidate composition and a new distinct pre-render review are pending.
- Do not use generated mock text/images as canonical content or logo assets.
- T-001 has a repaired fixture boundary awaiting new independent evaluation:
  discovery owner/path must be stated as absent when the exact source does not
  establish them, even if it names an adjacent role.

## 7. Exact next safe step

A distinct T-003 evaluator must inspect requester intent, the canonical source
locators, `evidence/T-003-editorial-map.md`, the three fixture candidates and
`evidence/T-003.md` against V-023-03/V-023-04/V-023-05 without editing.
Record APPROVE or REVISE. Do not start T-004 before an approval transitions
T-003 through the official state.

The prior T-003 review repair is already applied: reservoir recovery retains
the source-backed water-quality-lead authorization after the second sample and
marks physical scale unknown (with a required unit), not N/A or zero. Each
heterogeneous opening now exposes thesis, pillars, material limit and next
conversation. Re-run checks are recorded in `evidence/T-003.md`; the evaluator
must inspect the repair rather than treating this note as approval.

## 8. Resume reading order

1. `run-state.yaml`
2. `progress.md`
3. this handoff
4. repository status
5. current task and evidence
6. `validation-plan.md` and `decision-log.md`

## 9. Do not do

- Do not start T-004 before T-003 has independent approval.
- Do not change historical briefs or overwrite unrelated dirty work.
- Do not use deterministic scores to judge narrative depth or visual quality.
