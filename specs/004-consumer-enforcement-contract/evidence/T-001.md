# Evidence: T-001 — Consumer Human Visibility validator

**Task status:** approved  
**Builder:** Terra 5.6 implementation agent  
**Evaluator:** Terra 5.6 independent validation agent  
**Evaluator decision:** approve  
**Decision date:** 2026-08-18

## Scope and acceptance traceability

| Requirement / AC | Evidence |
|---|---|
| FR-001–004, FR-008; AC-001–004 | `scripts/validate_human_visibility.py` and `scripts/test_validate_human_visibility.py`. |

## Commands and results

| Command | Result |
|---|---|
| `python scripts/validate_human_visibility.py --help` | PASS; portable CLI exposes consumer root, initiative, base ref and baseline options. |
| `python scripts/test_validate_human_visibility.py` | PASS; valid fixture, absent brief, missing section, unresolved placeholder, missing source link/artifact, stale baseline, reviewed exception, Git stale/refreshed and invalid-ref fallback are covered. |

## Independent evaluation

The independent Terra evaluator initially rejected four gaps. After revision it
approved AC-001 through AC-004: Git failure now falls back to the local hash
baseline; reviewed YAML inline comments parse; source-link coverage exists; and
the output keeps structural, gate/state, freshness and HUMAN REVIEW categories
separate.

## Residual risk and limitation

The command deliberately does not judge brief meaning, stakeholder usefulness
or rendered legibility. A structural pass is not Human Visibility approval;
independent semantic/rendered review remains mandatory. Freshness depends on a
correct base ref or a baseline written after review, with explicit reviewed
exceptions for bounded cases.
