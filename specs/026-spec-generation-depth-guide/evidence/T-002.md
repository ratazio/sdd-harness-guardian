# Evidence — T-002 reference to the authoring-depth guide

**Initiative:** `026-spec-generation-depth-guide`  
**Task:** `T-002`  
**Builder ID:** `/root/spec026_workflow_reference`  
**Evaluator ID:** `/root/spec026_independent_evaluation` — distinct from Builder  
**Started/finished:** 2026-09-01  
**Working-tree description:** minimal reference added to the shared lifecycle  
**Decision:** approve

## 1. Implementation summary

The Common SDD Lifecycle now directs the author, at **Specify**, to read
`.harness/skills/spec-depth-authoring/SKILL.md` when that guide is available.
The reference says its questions deepen the existing canonical artifacts and
explicitly preserves its nature as guidance rather than an automated step or
semantic gate.

No command, hook, pipeline, parser, template/schema, renderer, skill content
or review-role file was added or changed by this task.

**Outcome served:** the future reusable guide is discoverable at the point a
SPEC is authored.

**Demonstrable increment:** reading the shared lifecycle's first step leads to
the expected guide path before drafting `spec.md`.

## 2. Files and contracts changed

| File/contract | Change | Why in scope |
|---|---|---|
| `.harness/workflows/sdd-lifecycle.md` | Added one conditional reference in the existing `Specify` entry. | This is the common authorship entry point for feature, bugfix and refactor workflows. |
| `specs/026-spec-generation-depth-guide/tasks.md` | Moved only T-002 to `needs_evaluation`. | Evidence is ready; independent approval remains required. |
| `specs/026-spec-generation-depth-guide/run-state.yaml` | Recorded only T-002's evaluation checkpoint. | Resumable state reflects the non-terminal task status. |

## 3. Acceptance and exit-criteria coverage

| AC/exit ID | Result | Validation ID | Evidence/output |
|---|---|---|---|
| AC-026-01 | approved | V-026-01 | Lifecycle `Specify` entry points to the single expected guide location. |
| AC-026-05 | approved | V-026-05 | Diff contains documentation/state/evidence only; it adds no semantic automation. |
| T-002 exit | approved | V-026-01, V-026-05 | Existing authoring flow leads to the guide without a new automated step. |

## 4. Validations executed

| Command/check | Environment/working dir | Exit/result | Relevant output/artifact |
|---|---|---|---|
| Manual lifecycle-path review | repository root | pass | `Specify` is the common entry used by the specialized SDD workflows. |
| Diff-scope review | repository root | pass | Only the lifecycle reference, T-002 state and this evidence file were introduced for T-002. |

## 5. Known gaps and residual risks

| Gap/risk | Severity | Owner/follow-up | Blocking? |
|---|---|---|---|
| Nenhuma lacuna material aberta; a skill existe no caminho referenciado e a avaliação foi aprovada. | low | `not_applicable` | no |

## 6. Builder handoff

**Implementation status:** complete  
**Requested evaluation:** confirm that the `Specify` reference is sufficient,
that it does not imply a new mandatory automation/gate, and that no unrelated
integration surface was changed.  
**Notes:** registro do builder; State Keeper registrou a transição terminal após `approve` independente.

## 7. Independent evaluation

**Evaluator identity:** `/root/spec026_independent_evaluation` (distinct from `/root/spec026_workflow_reference`).  
**Evaluator reviewed:** lifecycle `Specify` entry, guide target, SPEC 026, `manifest.yaml`, and integrity checks.  
**Findings:** none. The existing authorship path references the correct guide path conditionally and explicitly describes it as author guidance, not an automated workflow step or semantic gate. It creates no command, hook, automation, or renderer integration.  
**Decision rationale:** T-002 meets V-026-01 and V-026-05. `python scripts/validate_bundle.py` passed (282 checks), and `git diff --check` reported no error.  
**Decision:** `approve`.  
**Required next action:** State Keeper may record the terminal transition according to the workflow.

## 8. Terminal gate

- [x] Builder ID is recorded.
- [x] Evaluator ID is present and distinct (`/root/spec026_independent_evaluation`).
- [x] Outcome linkage and demonstrable increment are recorded.
- [x] Exit criteria are implemented and evidenced.
- [x] Evaluator decision is `approve`.
- [x] State Keeper may transition `approved -> done`.
