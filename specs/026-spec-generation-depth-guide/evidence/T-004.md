# Evidence — T-004: calibração proporcional

## Exemplo A — pedido M005 com arquitetura e contratos explicitamente fornecidos

**Fonte disponível:** pedido [M-005](../../../testes/mock-tests/05-local-ai-exam-scoring.md), sem inspeção de código.

- **spec.md:** o resultado é corrigir lotes de respostas discursivas com sugestão rastreável do modelo local; o escopo preserva `API origem → serviço → modelo local → API destino/MySQL`, e exclui acesso direto deste projeto ao MySQL, modelo externo, exportação de respostas por telemetria e treinamento com os dados recebidos.
- **impact-map.md:** dados educacionais pessoais/sensíveis atravessam API, serviço e modelo isolado; minimização/pseudonimização quando viável, criptografia, retenção, acesso, auditoria e logs mascarados são controles a decidir e evidenciar. Indisponibilidade do modelo e falha da API de destino ameaçam publicação; timeout, reenvio e idempotência reduzem esse risco.
- **plan.md / tasks.md:** validar lote, enfileirar trabalho, obter nota/justificativa estruturada, aplicar faixa determinística e publicar pela API de destino; manter versão de modelo/rubrica/prompt, confiança, bloqueio para revisão humana e reprocessamento. A separação entre controles determinísticos e avaliação probabilística permanece uma decisão de desenho, não promessa de precisão ou conformidade.
- **validation-plan.md:** usar contratos e dados sintéticos, invariantes de nota e simulação do modelo para lote inválido, ambiguidade, timeout, resposta fora da rubrica, publicação idempotente, falha de destino e reenvio; avaliação independente cobre privacidade, segurança e explicabilidade. **Descoberta D-M005-01:** o pedido exige regra de bloqueio humano, mas não define o limiar; dono: responsável de produto/qualidade; bloqueia a regra de aceite para publicação automática.

**Parecer independente: PASS.** As relações de fronteira, impacto sensível, sequência e prova sobrevivem sem presumir implementação; permanece o limite honesto do limiar humano.

## Exemplo B — pedido mínimo sem raiz, caminho ou localizador de código

**Pedido disponível:** “Criar uma SPEC para avisar clientes sobre manutenção.” Não foram fornecidos raiz de projeto, repositório, caminho, módulo, teste, contrato, canal de aviso ou critério de entrega.

- **spec.md:** intenção conhecida: definir o aviso de manutenção; atores, canal, conteúdo, gatilho e resultado mensurável permanecem desconhecidos. Nenhuma pasta, módulo, arquitetura ou integração é declarada.
- **impact-map.md:** **descoberta D-MIN-01:** identificar quem recebe o aviso, por qual canal e quem autoriza/envia; dono: solicitante; essa lacuna bloqueia delimitar impacto e dados envolvidos.
- **plan.md / validation-plan.md:** `not_applicable` para mudança de código e testes enquanto não houver fonte ou decisão que os indique; próximo passo é obter essas informações, não buscar para eleger estrutura técnica.

**Parecer independente: REVISE.** Pedido mínimo → ausência de atores/canal/critério → não é possível decidir escopo, risco ou validação → recuperar no `spec.md` e registrar a decisão em `decision-log.md` com o solicitante.

## Independent evaluation

**Evaluator identity:** `/root/spec026_independent_evaluation` (distinct from the builders of T-004).  
**Evaluator reviewed:** both Markdown examples, T-004 scope/exit criteria, SPEC 026, validation plan V-026-04..05, guide, reviewer role, workflow, and integrity checks.  
**Findings:** none. There are exactly two Markdown examples: M005 with an explicitly supplied architecture/contracts context and a minimal request with no repository, path, or locator. The first preserves architecture, impact, execution, validation, and a named unresolved decision without asserting code inspection; the second declares a bounded discovery and illustrates the required `REVISE` chain without inventing technical structure. No mock, complete SPEC, HTML, or automation is created.  
**Decision rationale:** T-004 meets V-026-04 and V-026-05 proportionally. `python scripts/validate_bundle.py` passed (282 checks), and `git diff --check` reported no error.  
**Decision:** `approve`.  
**Required next action:** State Keeper may record the terminal transition according to the workflow.

## Terminal gate

- [x] Builder and evaluator are distinct; evaluator identity is `/root/spec026_independent_evaluation`.
- [x] Exactly two Markdown examples satisfy the T-004 scope and exit criteria.
- [x] No mock, complete SPEC, HTML, or unrelated automation was introduced by this evidence.
- [x] Evaluator decision is `approve`.
- [x] State Keeper may transition `approved -> done`.
