# Evidence — T-003: Revisor independente de profundidade

**Task status:** `done`  
**Builder identity:** `/root/spec026_reviewer_role`  
**Required evaluator:** identidade distinta do builder e do autor da SPEC  
**Scope:** papel/prompt reutilizável; sem alteração de renderer, HTML, templates
visuais, implementação ou aprovação de task.

## Entrega

`.harness/agents/spec-depth-reviewer.md` define a identidade
`spec-depth-reviewer` e exige como entrada o pedido original, `spec.md` e
`plan.md`. A saída está limitada a `PASS` ou `REVISE`; para `REVISE`, o papel
prescreve o encadeamento literal:

```txt
fonte → perda/deformação ou risco → decisão prejudicada → correção canônica
```

O papel é distinto do autor, revisa proporcionalmente relações materiais e
aceita ausência justificada. Ele veta score, quotas e invenção de
caminhos/arquitetura, e declara que não reescreve SPEC, não implementa e não
aprova task.

`manifest.yaml` registra o novo papel junto aos agentes convencionais do
bundle.

## Checks executados

| Check | Evidência | Resultado |
|---|---|---|
| Entrada e decisão | Inspeção de `spec-depth-reviewer.md`: pedido + `spec.md` + `plan.md`; decisão única `PASS`/`REVISE`. | PASS |
| Achado acionável | Inspeção do formato literal fonte → perda/deformação ou risco → decisão prejudicada → correção canônica. | PASS |
| Limites qualitativos | Inspeção: ausência justificada aceita; score, contagens e quotas vetados. | PASS |
| Não invenção / não implementação | Inspeção: veda busca por semelhança, caminhos/arquitetura imaginados, reescrita, implementação e aprovação de task. | PASS |
| Registro do bundle | `manifest.yaml` inclui `spec-depth-reviewer`. | PASS |
| Escopo | Diff limitado ao papel, manifesto, estado de T-003 e esta evidência. | PASS |

## Limitações e próximo gate

Esta evidência comprova o contrato documental do papel, não a qualidade de uma
revisão aplicada. A calibração em exemplos Markdown é de T-004 e foi avaliada
separadamente. A identidade independente aprovou esta entrega; o State Keeper
registrou T-003 como `done`.

## Independent evaluation

**Evaluator identity:** `/root/spec026_independent_evaluation` (distinct from `/root/spec026_reviewer_role` and the SPEC author).  
**Evaluator reviewed:** reviewer role, SPEC 026 FR-026-06..08, authoring guide boundaries, workflow, manifest, and integrity checks.  
**Findings:** none. The role requires a distinct identity and the original request, `spec.md`, and `plan.md`; its only outcomes are `PASS` or `REVISE`, with the required source → loss/risk → impaired decision → canonical correction chain. It remains qualitative and proportional, accepts justified absences, and excludes scoring, quotas, speculative inspection, rewriting, implementation, and task approval.  
**Decision rationale:** the role satisfies V-026-03 and preserves the stated boundaries. `python scripts/validate_bundle.py` passed (282 checks), and `git diff --check` reported no error.  
**Decision:** `approve`.  
**Required next action:** State Keeper may record the terminal transition according to the workflow.

## Terminal gate

- [x] Builder and evaluator are identified and distinct (`/root/spec026_reviewer_role` / `/root/spec026_independent_evaluation`).
- [x] FR-026-06..08 and T-003 exit criteria are independently approved.
- [x] Evaluator decision is `approve`.
- [x] State Keeper may transition `approved -> done`.
