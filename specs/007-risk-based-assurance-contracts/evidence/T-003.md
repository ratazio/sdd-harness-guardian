# Evidence Pack: T-003

**Initiative:** 007-risk-based-assurance-contracts  
**Task:** T-003  
**Builder ID:** Codex / Builder-007-T003  
**Evaluator ID:** Codex / Evaluator-007-T003  
**Decision:** approve

## Implementation summary

Added a clearly labelled fictional 006 derivative, a single local structural
mirror and precise positive/negative fixtures. Historical `specs/006-*` is not
modified. The mirror checks only opted-in exact profile, non-empty rationale
and non-empty A2/A3 source pointer; it says explicitly that semantic adequacy
requires independent review.

## Acceptance evidence

| Claim | Artifact/result |
|---|---|
| A2 current/target/delta/envelope | `positive-plan.md`; profile and envelope are in existing plan form |
| Risk/control/task contract | `fictional-task.md` plus `stakeholder-brief.html` |
| A1 proportionality | `a1-local.md` passes with concise N/A disposition |
| Invalid/partial structure blocks | missing rationale, invalid profile and empty A2 link fixtures fail precisely |
| Waiver/failure behavior | fixture task requires human/reason/risk/control/scope/expiry and says incomplete waiver blocks done |
| Brief disclosure | fixture brief has current/target/delta, risk ledger, individual A1/A2 tasks and proof states |

## Executed commands

| Command | Result |
|---|---|
| `python scripts/test_validate_assurance_contract.py` | positive A2/A1 pass; 3 targeted negative fixtures fail |
| `python scripts/validate_bundle.py` | pass |
| `git diff --check` | pass |
| `git diff --name-only -- specs/006-*` | empty; historical 006 unchanged |

## Render and review rubric

The fixture is standalone, responsive by viewport and no-script (no script is
used). Review desktop/narrow/print by opening `stakeholder-brief.html`: confirm
tables stay readable, keyboard reaches `details` where used, every risk/task is
recoverable, and the historical-fictional label remains visible. This is a
manual independent-review requirement, not a screenshot pixel gate.

## Rendered artifact manifest

| View | Artifact | Expected review |
|---|---|---|
| desktop | `scripts/fixtures/assurance-contracts/fictional-006/stakeholder-brief.html` | full ledger and both task rows visible |
| narrow | same responsive, no-script HTML | tables remain horizontally recoverable; disclosure remains keyboard reachable |
| print | same semantic HTML | all decision sections retain content without script |
| no-script | same HTML, no JavaScript embedded | all sections, ledgers and disclosures are source-visible without scripting |

No browser script is embedded, so no-script and print behavior are source-verifiable; the independent evaluator performs the visual reading rather than a pixel-diff gate.

## Minimal-core audit

**Failure class:** declared elevated work has no recoverable rationale/source
pointer. **Why review alone:** repeated, stable omission across distributed
Markdown. **Cost:** one standard-library script and fixtures. **Failure:**
diagnostic blocks the focused check, never asserts adequacy. **Removal:** demote
or remove after two supported consumer uses demonstrate unstable representation
or maintenance/false-positive cost exceeds recurring omissions prevented.

## Independent evaluation

Evaluator-007-T003 approved the fixture, exact mirror, three negative cases,
ledger expansion and no-script manifest after focused tests, bundle validation,
diff check and historical-006 isolation passed.
