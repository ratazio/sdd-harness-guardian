# T-003 — Refusals and non-semantic lifecycle limits

**Status:** done  
**Builder:** `spec022_t003_builder` (Terra, medium)  
**Independent evaluator:** `spec022_t003_evaluator` (Terra, medium; distinct
from builder)  
**Decision:** `decision-log.md#D-022-030`

## D-022-035 — D-022-034 recovery independently approved

**Builder:** `spec022_postrender_authority_builder`. **Independent evaluator:**
`/root/evaluate_d022034_repair` (distinct from the builder). **Verdict:**
APPROVE.

The evaluator approved the generic, opt-in recovery boundary introduced for
D-022-034: an explicit `status: blocked` checkpoint takes precedence over
historical review context, declared lifecycle spans project that checkpoint,
and guarded refresh refuses until the state is explicitly `executing`. The
finding remains **2 — lack of proof**; it was not a semantic-quality or
SPEC-specific layout problem.

The approval closes T-003's recovery work only. It does not approve the
refused rendered target, bind the retained candidate, approve a pre-render or
post-render review, advance Human Visibility or Tasks Ready, start T-004,
create a baseline, deliver a brief, or alter SPEC 021. A source-first
recomposition with `brief_review` pending now requires a new independent exact
candidate review.

## D-022-032 — Source-manifest repair independently approved

**Builder:** `spec022_t003_authority_builder`. **Independent evaluator:**
`/root/evaluate_source_manifest_repair` (distinct from the builder).
**Verdict:** APPROVE.

The evaluator independently approved the repair that returns the canonical
lifecycle sources to `ready_to_render` / `render_pending` with
`brief_review.findings_status: pending`. The repair preserves the closed
lifecycle projection: this state means a fresh authored candidate is awaiting
pre-render review, not that it is rendered or deliverable.

Boundary: this is a T-003 repair approval only. It does not approve the
historical rendered target, any candidate, rendered-decision review, Human
Visibility, Tasks Ready, T-004, delivery or SPEC 021. A fresh composition and
distinct exact pre-render review remain required before guarded refresh.

## D-022-030 — Closed-gate repair independently approved

**Builder:** `spec022_t003_authority_builder` (Terra, medium).  
**Independent evaluator:** `/root/evaluate_closed_gate_contract` (distinct
from the builder). **Verdict:** APPROVE.

The evaluator independently approved the systemic repair after the P1s. The
closed lifecycle-authority derivation remains the only authority source: it
uses structured lifecycle and review state, refuses incompatible or stale
declarations before render, and does not infer or rewrite domain prose. The
closed gate-outcome policy also remains explicit: a task-level repair approval
does not bind or approve a candidate, rendered target, Human Visibility, Tasks
Ready, T-004, delivery, or SPEC 021.

The evaluator confirmed the required suites passed:

```text
python scripts/test_render_stakeholder_brief.py             PASS
python scripts/test_source_render_isolation.py              PASS
python scripts/test_validate_human_visibility.py            PASS
python scripts/test_decision_quality_review_contract.py     PASS
python scripts/validate_bundle.py                           PASS (272 checks)
```

The current candidate remains SHA-256
`7af7adf757a28621f5417ac6ae1adcce75cf7bfe22e1b129131ec93bd8747353`,
authored and pending a new independent pre-render review. No review record
PASS binds that SHA; it is not promotion-authorized.

## Current reopen — D-022-029

The historical approvals in this evidence do not approve the current repair.
The manual `lifecycle_authority_text` scalar was removed as an authority source.
Markers and source spans now declare only `projection="lifecycle-authority"`;
the renderer derives one of three closed states from `brief_phase`,
`current_phase`, and structured `brief_review` linkage where applicable.

The focused regression reproduces the evaluator P1: pending `brief_review`
metadata, a mismatched decision-record candidate SHA, and a passed-looking
marker. Promotion refuses before rendering because the marker must match the
computed pending projection. Varied source layouts and non-marker byte
preservation remain covered. The repaired candidate is
`7af7adf757a28621f5417ac6ae1adcce75cf7bfe22e1b129131ec93bd8747353`.

Builder checks passed; the subsequent independent approval is recorded in
D-022-030. That task-level decision still does not authorize promotion,
rendered-decision review, Human Visibility, Tasks Ready, T-004, or SPEC 021.

## Delivered increment

The promotion transaction now admits only the declared staging phase
`current_phase: "render_pending"` and writes
`current_phase: "rendered_decision_review_pending"` together with
`brief_phase: "rendered"`. The state and HTML remain one journal/backup guarded
pair. This makes the next gate truthful after successful promotion without
rewriting `progress.md`, the handoff or authored decision prose.

## Independent evaluation

Verdict: **APPROVE**. The evaluator confirmed that the renderer transitions
only the two lifecycle scalars, refuses `specify`, already-rendered-review and
blocked phases, preserves old-or-new recovery at every injected commit/recovery
fault, and adds no score, materiality/domain inference, prose rewrite or new
approval role/stage.

## Commands

Builder, independent evaluator and orchestrator ran:

```text
python scripts/test_render_stakeholder_brief.py             PASS
python scripts/test_source_render_isolation.py              PASS
python scripts/test_validate_human_visibility.py            PASS
python scripts/test_decision_quality_review_contract.py     PASS
python scripts/validate_bundle.py                           PASS (272 checks)
```

## Coverage-authority correction — D-022-019

**Builder:** `spec022_t003_coverage_fix` (Terra, medium).  
**Independent evaluator:** `spec022_t003_coverage_evaluator` (Terra, medium;
distinct from builder). **Verdict:** APPROVE.

The earlier post-render attempt exposed a real structural conflict: the
coverage authority marker replaced the factual coverage-disposition cell. The
marker now appears as a direct-text `<p>` immediately after the coverage table,
so the table retains its `represented`/`synthesized`/N/A disposition while
rendering still derives only the fixed lifecycle statement. No domain prose,
semantic score, materiality inference or new gate was added.

Builder and evaluator passed renderer, Human Visibility contract, source
isolation, decision-quality contract and bundle validation (272 checks). The
target rendered before this repair is historical/refused; a fresh candidate
review and guarded refresh remain required.

## Boundary

This approval does not approve the historical rendered target, the D-022-012
candidate, Human Visibility, Tasks Ready, T-004 or SPEC 021. Those require a
fresh source-bound composition and rendered-decision review.

## Reopened after rendered-decision review — D-022-016

Status is now **needs_revision**. Architect and system-designer review of the
promoted D-022-015 target found that scalar lifecycle state transitioned
correctly but static candidate/refresh authority copy remained visible. The
next revision is limited to explicitly declared lifecycle authority-text
markers; it must not rewrite arbitrary authored prose or automate semantic
judgment. The earlier evaluator approval remains evidence for the scalar-pair
portion only, not for final authority projection.

## Authority-projection revision — D-022-017

**Builder:** `spec022_t003_authority_builder` (Terra, medium).  
**Independent evaluator:** `spec022_t003_authority_evaluator` (Terra, medium;
distinct from builder). **Verdict:** APPROVE.

Six explicit, unique and location-bound lifecycle authority markers replace
only direct candidate text at successful promotion with: “Rendered artifact;
rendered-decision review pending; not approved/deliverable; Tasks Ready false.”
Unknown, duplicate, misplaced, nested and stale markers refuse. No source
prose, handoff, progress, semantic score or materiality decision is rewritten.

Evaluator and orchestrator passed:

```text
python scripts/test_render_stakeholder_brief.py             PASS
python scripts/test_source_render_isolation.py              PASS
python scripts/test_validate_human_visibility.py            PASS
python scripts/test_decision_quality_review_contract.py     PASS
python scripts/validate_bundle.py                           PASS (272 checks)
```

## Reopened P1 and systemic authority contract — D-022-022 / D-022-023

**Status:** reopened after the rendered target review, then independently
approved after correction. The target SHA-256
`77027bd5d517bd309f5766afedc6bf43a99ed86ff16e0dbd1bdb30cf73c19386`
remains refused and is not a delivery artifact.

The P1 was not treated as a wording patch. The first narrow attempt exposed
why a list of authority slots such as hero, progress or coverage is not a
reusable contract: it models one SPEC layout and can reintroduce the factual
coverage-disposition loss already corrected by D-022-019.

The approved correction replaces that catalog with one repeated,
author-declared `rendered-authority` marker. It is opt-in per direct-text
element, binds to `run-state.yaml` and a nonempty provenance fragment, and can
occur in any body layout. Promotion changes only that declared text to the
fixed rendered authority. It refuses missing, nested, outside-body, duplicate
or undeclared lifecycle declarations and rejects use inside the v2
`#coverage-register`, where cells remain factual source-coverage records.
This is a transformation boundary, not an attempt to infer candidate language,
domain meaning, or the structure of future specifications.

**Builder:** `spec022_t003_authority_builder` (Terra, medium).
**Independent evaluator:** `spec022_t003_authority_evaluator` (Terra,
medium; distinct from builder). **Verdict:** APPROVE.

The evaluator reviewed generic fixtures with different tags, layouts and
pre-render wording, byte preservation outside explicit markers, the SPEC 022
P1 reproduction and coverage-register protection. It found no phrase catalog,
SPEC-specific locator, semantic score, materiality inference, new role or
stage. This approval covers the reusable contract only; it does not approve a
candidate, rendered review, Human Visibility, Tasks Ready, T-004 or SPEC 021.

Builder, evaluator and orchestrator ran the required regressions:

```text
python scripts/test_render_stakeholder_brief.py             PASS
python scripts/test_source_render_isolation.py              PASS
python scripts/test_validate_human_visibility.py            PASS
python scripts/test_decision_quality_review_contract.py     PASS
python scripts/validate_bundle.py                           PASS (272 checks)
```

## Source-lifecycle repair — D-022-026

**Status:** T-003 remains done; this is a recorded regression repair, not a
new approval of a rendered artifact. **Builder:** `spec022_t003_authority_builder`
(Terra, medium). **Independent evaluator:**
`spec022_t003_authority_evaluator` (Terra, medium; distinct from builder).
**Verdict:** APPROVE.

The P1 showed that an atomic state/HTML pair is insufficient when canonical
operational sources themselves retain pre-render statements after promotion.
The repair extends the same closed, declarative source authority span to the
canonical lifecycle claims in `progress.md` and the latest handoff. It changes
only direct text inside opt-in spans bound to `run-state.yaml`; it does not
search for lifecycle words, classify domain prose, prescribe a document
layout, or rewrite arbitrary Markdown.

The evaluator verified generic source fixtures with varied phrasing and
layouts, refusal of malformed/foreign/nested declarations, byte preservation
outside declarations, and an end-to-end P1 reproduction. The historical HTML
target remains refused. The state was then returned source-first to
`ready_to_render` / `render_pending` for a fresh authored candidate, so the
next guarded refresh can create a new coherent pair.

## P1 reopened — D-022-028

The later review found that a PASS-bound candidate could retain declared direct
authority text saying the review was pending. The repair requires every opted-in
HTML/source authority declaration to match the declared run-state scalar before
promotion, then advances that scalar atomically. This records a pending repair,
not a rendered decision, Human Visibility, Tasks Ready or delivery approval.
