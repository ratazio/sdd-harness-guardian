# T-001 — Contrato mínimo atômico de lifecycle

**Status:** needs_revision. **Builder:** `spec022_t001_builder` (Terra,
medium). **Evaluator r1:** `spec022_t001_evaluator` (Terra, medium; distinct
from builder). **Task is not done.**

## Builder increment (r1)

The builder changed only `scripts/render_stakeholder_brief.py` and
`scripts/test_render_stakeholder_brief.py`. The proposed increment added a
closed lifecycle-marker surface, final-state digest, visible pending-review
authority, journal/backup promotion and fault-injection tests. It reported the
focused suite and `validate_bundle.py` passing.

## Independent evaluation r1 — REVISE P1

The evaluator independently reran:

```text
python scripts/test_render_stakeholder_brief.py
python scripts/test_source_render_isolation.py
python scripts/test_validate_human_visibility.py
python scripts/test_decision_quality_review_contract.py
python scripts/validate_bundle.py
```

All commands passed, but the implementation is not approved because these
adversarial requirements failed:

1. `lifecycle_error()` accepted duplicate reserved attributes, an unknown
   second marker ID, conflicting source bindings, undeclared lifecycle
   attributes and `rendered-authority` in `<head>`.
2. A nested authority marker such as `<p> <span>candidate</span> </p>` passed
   structural checks while leaving stale visible authority after render.
3. Interrupted promotions left untracked UUID temporary files because the
   journal and recovery protocol did not enumerate/clean them.

The evaluator therefore marked V-022-01, V-022-02 and V-022-03 **not
satisfied**. It confirmed the positive constraints already present: final
state digest in the normal path, explicit `--refresh`, no automatic Human
Visibility/Tasks Ready mutation, and no semantic/domain classifier.

## Required r2 evidence

- adversarial refusals for every duplicate/unknown/misplaced/undeclared marker
  attribute and nested authority content;
- end-to-end visible authority exactness;
- all transaction artifact paths recovered or safely refused, with no residue
  after every declared injected boundary;
- byte-level proof that non-allowlisted content is unchanged;
- fresh independent run of the five commands above and a new evaluator verdict.

No status, gate or baseline is advanced by this draft evidence.

## Independent evaluation r2 — REVISE P1

Evaluator r2 was again distinct from the builder and independently reran the
same five commands; all passed, including `validate_bundle.py` (`272 checks`).
The evidence remains unapproved because two adversarial checks still failed:

1. `rendered-authority` was accepted nested under `<body>` and with nested
   content. The rewriter then left `<span>candidate</span>` visible, so V-022-01
   and V-022-02 were not satisfied.
2. With no journal, `recover_promotion()` ignored a stale pre-journal
   `.<target>.*.tmp`. That violates cleanup/refusal before exposure, so
   V-022-03 was not satisfied.

The builder is correcting only direct-child/direct-text authority location and
safe discovery of pre-journal transaction residue. A third evaluator must
repeat the adversarial cases and commands. T-001 remains `in_progress`; no
task/gate/baseline advances.

## Independent evaluation r3 — REVISE P1

Evaluator r3 independently ran all five required commands; all passed and the
bundle still reported `272 checks`. It approved V-022-01 (closed markers,
location/nesting/attribute refusals, final state/provenance binding and exact
allowlisted-byte rewrite) and V-022-02 (exact pending authority, no automatic
visibility/Tasks Ready and no semantic classifier).

V-022-03 remains failed. A syntactically valid but malformed journal could
list `.unrelated.tmp` in its `temps` array. When the live pair matched the
intent, recovery deleted that unrelated file. The correction must validate the
two exact transaction-shaped, target/state-prefixed UUID temporary names (and
their common operation nonce) before any cleanup; otherwise recovery must
refuse without deleting it. T-001 is still not approved.

## Independent evaluation r4 — APPROVE

**Evaluator:** `spec022_t001_evaluator_r4` (Terra, medium; distinct from
`spec022_t001_builder`). The evaluator approved T-001 after independently
inspecting the r4 renderer and running the required commands.

| Validation | Result | Evidence |
|---|---|---|
| V-022-01 | PASS | closed unique markers; exact direct-body authority; final raw-state digest and current run-state provenance; byte comparison changed only four allowlisted lifecycle values |
| V-022-02 | PASS | visible text states composition complete, rendered-decision review pending and `Tasks Ready: false`; no automatic Human Visibility/Tasks Ready or semantic classifier |
| V-022-03 | PASS | every `COMMIT_POINTS` boundary and idempotent recovery; unrelated/missing/duplicate/nonmatching-nonce journal temps refused without deletion; only an exact target/state common-nonce pair is cleaned |

Independent required commands:

```text
python scripts/test_render_stakeholder_brief.py                         PASS
python scripts/test_source_render_isolation.py                          PASS
python scripts/test_validate_human_visibility.py                        PASS
python scripts/test_decision_quality_review_contract.py                 PASS
python scripts/validate_bundle.py                                       PASS (272 checks)
```

The delivery orchestrator reran the same five commands after the evaluation;
all passed with the results above. Scope stayed within
`scripts/render_stakeholder_brief.py` and
`scripts/test_render_stakeholder_brief.py`; no SPEC 021 artifact, lifecycle
gate or baseline was changed.

**Final verdict:** APPROVE. T-001 is done. This completes only the
user-authorized bootstrap repair and restores the normal brief workflow for
the next task; it does not approve T-002–T-004 or any rendered brief.
