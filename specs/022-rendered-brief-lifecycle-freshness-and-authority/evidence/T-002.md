# T-002 — Integração mínima de `ratchet.md` no contrato v2

**Status:** done. **Builder:** `spec022_t002_builder` (Terra, medium).
**Evaluator r1:** `spec022_t002_evaluator` (Terra, medium; distinct from
builder). The historical r1 verdict below is superseded by r4 APPROVE.

## Authorized outcome

The user authorized only one reconciliation: make the same static local
`ratchet.md` source consistently admissible in renderer provenance, v2 coverage
and baseline freshness. The authorization excludes content interpretation,
scoring, roles/stages, arbitrary evidence/handoff paths and all later tasks.

## Independent evaluation r1 — REVISE P1

The evaluator found the narrow ratchet behavior directionally correct and
independently passed:

```text
python scripts/test_validate_human_visibility.py
python scripts/test_render_stakeholder_brief.py
python scripts/test_source_render_isolation.py
python scripts/test_decision_quality_review_contract.py
python scripts/validate_bundle.py  # 272 checks
```

It nevertheless withheld approval because the static source set remained
duplicated across renderer, validator and tests, recreating the drift that
caused the defect. A single importable source-contract definition is required.

The evaluator also observed extensive pre-existing validator/test changes that
are outside T-002. They must not be reverted or claimed as this task's work;
the r2 evaluation must isolate T-002 to its source-contract module, imports
and focused ratchet regressions.

## Required r2 evidence

- one ordered source-contract definition consumed by renderer, validator and
  relevant tests, while composition-review core sources remain unchanged;
- ratchet absent block/row/invalid disposition/stale source digest/stale
  fragment digest/unknown source/stale baseline negatives;
- no semantic, materiality, severity, prose, role/stage or path-scope change;
- fresh distinct evaluation and the five command results.

## Independent evaluation r2 — REVISE P1

Evaluator r2 independently passed all five required commands (bundle: `272`
checks) and confirmed that the ratchet negatives and anti-scope boundary are
otherwise correct. It did not attribute the pre-existing broad dirty validator
work to T-002.

The one remaining P1 is source-set drift: the new shared module defines the
full ordered v2 list, but the renderer imported only the support suffix and
rebuilt its admission list from eight literals in a different order. T-002
must import and derive renderer admission from `V2_REQUIRED_SOURCES` directly,
while retaining the separate five-source composition-review digest contract.
No task/gate/baseline advances until r3 approval.

## Independent evaluation r3 — REVISE P1

Evaluator r3 confirmed the shared source contract, direct renderer derivation,
unchanged five-source composition review contract and all focused negatives.
The five required commands again passed. It found one actual anti-scope breach:
the existing evidence-reference scanner iterated the expanded required-source
tuple, so adding `ratchet.md` caused the scanner to apply evidence/handoff path
restrictions to ratchet text for the first time.

That policy expansion is not necessary to validate ratchet provenance,
coverage or baseline freshness. The correction must preserve the original
eight-source evidence-reference scan explicitly, exclude the support-source
suffix, and prove that evidence-like text in `ratchet.md` cannot activate it.
This narrows the implementation rather than adding a rule.

## Independent evaluation r4 — APPROVE

**Evaluator:** `spec022_t002_evaluator_r4` (Terra, medium; distinct from
`spec022_t002_builder`). The evaluator approved the final narrow integration:

- `scripts/brief_v2_sources.py` is the one ordered static contract: the
  original eight evidence-reference sources, the single `ratchet.md` support
  source, and their required-source union;
- renderer provenance admission derives directly from that union while its
  five-source composition-review digest contract is unchanged;
- v2 validator coverage/provenance/freshness baseline include ratchet, but its
  evidence-reference scanner remains explicitly limited to the original eight;
- focused negative coverage proves absent block/row, invalid disposition,
  stale source and fragment bindings, stale source-set baseline, unknown source
  and evidence-like traversal text in ratchet that does not activate the
  unrelated scanner.

No semantic scoring, materiality/severity inference, prose/role/stage rule,
arbitrary path policy, SPEC 021 change or later task was attributed to T-002.
Pre-existing broad dirty validator/test work was preserved and excluded.

Independent and orchestrator reruns both passed:

```text
python scripts/test_validate_human_visibility.py             PASS
python scripts/test_render_stakeholder_brief.py              PASS
python scripts/test_source_render_isolation.py               PASS
python scripts/test_decision_quality_review_contract.py      PASS
python scripts/validate_bundle.py                            PASS (272 checks)
```

**Final verdict:** APPROVE. T-002 is done. It removes a source-contract
contradiction; it does not make a stakeholder brief sufficient by script.
