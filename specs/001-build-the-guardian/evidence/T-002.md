# Evidence Pack: T-002

**Initiative:** build-the-guardian  
**Task:** T-002 — Make templates and tooling operational  
**Builder ID:** codex-root  
**Evaluator ID:** codex-independent-evaluator-2 (cycle 1: codex-independent-evaluator-1)  
**Date:** 2026-07-13  
**Working tree:** initial repository source is untracked; no baseline commit  
**Decision:** approve

## 1. Implementation summary

Revised every required artifact template, added direct `run-state.yaml`,
template copy guidance, bug reproduction and ratchet log templates, plus
optional stdlib-only scaffolding and structural validation scripts.

## 2. Files and contracts changed

- every file in `.harness/templates/`;
- `scripts/new_initiative.py`;
- `scripts/validate_bundle.py`;
- manifest artifact-template registry.

## 3. Acceptance and exit-criteria coverage

| AC/exit ID | Result | Validation | Evidence |
|---|---|---|---|
| AC-004 | pass | V-002 | feature and bugfix artifacts created in isolated temp root |
| AC-004/EC-001 | pass | V-003 | duplicate exited 1; spec SHA-256 unchanged |
| AC-005 | pass | V-001/V-004 | direct YAML keys and no Markdown fence |
| stdlib syntax | pass | V-004 | three Python files parsed by AST |

## 4. Validations executed

| Command/check | Exit/result | Relevant output |
|---|---|---|
| `python scripts/smoke_test_scaffolder.py` | 0 | `artifacts/scaffold-smoke.txt` |
| feature scaffold subprocess | 0 | complete canonical feature tree created |
| bugfix scaffold subprocess | 0 | reproduction file and `initiative_kind: "bugfix"` created |
| duplicate scaffold subprocess | 1 expected | refusal; before/after SHA-256 `15e904629c85610a8879d9c3f1fb3ec8075353d20388254e97e1faa84d1ac74e` |
| AST parse of `scripts/*.py` | 0 | `Python syntax passed: 3 files.`; `artifacts/validation-output.txt` |
| TemporaryDirectory cleanup | 0 | isolated root removed after assertions |

## 5. Skipped checks

Real consumer Git submodule checkout was not run. `origin` is configured, but
there is no local HEAD/tag and a published pinned install was not exercised.
This does not affect local scaffold behavior and remains a release pilot risk.

## 6. Known gaps and residual risks

Python is optional; manual copying is documented. Runtime-specific YAML schema
enforcement remains a consumer hard mirror recommendation.

## 7. Builder handoff

**Implementation status:** complete  
**Requested evaluation:** inspect overwrite safety, path isolation, complete
template contract and absence of workflow-engine behavior.

## 8. Independent evaluation

Cycle 1: `request_revision` because smoke evidence was not reproducible.
`scripts/smoke_test_scaffolder.py` and retained output resolve the finding.
Cycle 2: `approve`; see `artifacts/evaluation-cycle-2.md`.
