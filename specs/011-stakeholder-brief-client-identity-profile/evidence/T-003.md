# Evidence Pack: T-003

**Task:** Apply semantic operational variants to brief components  
**Builder:** t003_builder  
**Evaluator:** t003_visual_review (independent of builder)  
**Status:** done — approved in D-014  
**Date:** 2026-08-26  
**Working-tree state:** uncommitted task changes in the shared bundle worktree.

## Delivered increment

- The reusable template exposes Pearson-only variants for the header, tabs,
  snapshot, architecture, task, impact, risk, proof, decision and coverage
  roles through `html[data-client-identity-profile="pearson"]`.
- The selected 011 brief explicitly declares `data-client-identity-profile="pearson"`,
  renders the local 175 × 53 white logo in a native labelled link, and has no
  `role="img"` on that anchor.
- Impact footprint, risk/control chains, proof cards and grouped provenance add
  text labels and semantic structure while retaining the canonical tables and
  provenance register. Narrow layouts reflow the new groups; reduced motion is
  preserved.

## Files changed

- `.harness/templates/stakeholder-brief.html`
- `specs/011-stakeholder-brief-client-identity-profile/stakeholder-brief.html`
- `scripts/test_client_identity_profile_contract.py`

## Commands and results

| Command | Result | Oracle |
|---|---|---|
| `python scripts/test_client_identity_profile_contract.py` | PASS (exit 0) | opt-in boundary, local verified logo, semantic variants and no hotlink. |
| `python scripts/test_brief_v2_contracts.py` | PASS (exit 0) | v2/provenance contract remains intact. |
| `python scripts/test_tabbed_brief_surface.py` | PASS (exit 0) | tabs and no-script fallback remain intact. |
| `git diff --check` | PASS; only pre-existing CRLF warning for `specs/INDEX.md` | no whitespace error in the task diff. |

## Acceptance and exit-criteria traceability

| Criterion | Evidence and result |
|---|---|
| FR-004 / AC-004 | Focused test verifies named semantic roles; selected brief applies differentiated header, tabs, task, impact, risk, proof, decision and coverage treatments. |
| FR-005 / AC-005 | Footprint/risk/control and provenance groups are rendered alongside the existing impact and coverage tables; source labels and tables remain available. |
| FR-006–FR-007 / AC-006–AC-007 | The implementation includes text/status labels, structure, narrow reflow and reduced motion. Full keyboard, zoom, print and grayscale mode proof is intentionally deferred to T-004. |
| FR-011 / AC-010 | The implementation uses current v2 semantic hooks without changing canonical sources/gates; independent visual judgment is still required. |
| T-003 exit criteria | Material views receive an appropriate variant and semantic equivalents remain. Independent visual acceptance is pending. |

## Known gaps and residual risk

This pack does not claim browser-mode, grayscale, print, 200% zoom, no-network
or final semantic/release validation. Those are explicit T-004 responsibilities.
The independent evaluator must assess visual proportionality, readability and
fidelity before this task can be approved.

## Independent evaluator decision

`t003_visual_review` independently inspected the diff, evidence and rendered
selected brief without editing it, then re-ran the focused contract, v2, tabbed
surface and whitespace checks. The evaluator's decision is **approve** in
`decision-log.md#D-014`: desktop and 320px render show an operational—not
marketing—surface; the logo remains local/proportional in a native link;
impact, proof and coverage preserve text labels, tables and provenance.

No blocking or non-blocking finding applies to T-003. The evaluator confirms
that keyboard, print, grayscale, 200% zoom, no-network and final
release/isolation proof remain T-004 scope, not an unsubstantiated T-003 claim.
