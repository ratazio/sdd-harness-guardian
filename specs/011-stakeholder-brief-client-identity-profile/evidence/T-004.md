# Evidence Pack: T-004

**Task:** Prove accessibility, offline fallback and profile isolation  
**Builder:** t003_builder  
**Evaluator:** t004_release_review (independent of builder)  
**Status:** done — D-018 independent approval and D-019 terminal synchronization  
**Date:** 2026-08-26  
**Working-tree state:** shared uncommitted bundle changes, including the T-004 render test.

## Delivered increment

- `scripts/test_client_identity_profile_render.py` uses a local HTTP server and
  headless Chromium to prove selected/default request isolation, selected logo
  semantics, responsive widths, 200% zoom, keyboard tab navigation, reduced
  motion, no-script panels, print-PDF generation and text/structural risk and
  coverage cues.
- The test observes the selected brief and the neutral template separately:
  only the selected profile requests the local Pearson PNG; no request crosses
  the local server boundary.

## Files changed

- `scripts/test_client_identity_profile_render.py`
- Any template/brief/test changes already listed in T-002/T-003 evidence are
  consumed as the validated surface; T-004 does not add a remote dependency.

## Commands and results

| Command | Result | Oracle |
|---|---|---|
| `python scripts/test_client_identity_profile_render.py` | PASS (exit 0) | selected/default local isolation; 320/390/768/1440; 200% zoom; keyboard; reduced motion; print PDF; no-script; text/structure. |
| `python scripts/test_client_identity_profile_contract.py` | PASS (exit 0) | opt-in boundary, checksum/dimensions, local logo, semantic variants and no hotlink. |
| `python scripts/test_brief_v2_contracts.py` | PASS (exit 0) | v2/provenance contract remains intact. |
| `python scripts/test_tabbed_brief_surface.py` | PASS (exit 0) | tabs and no-script fallback remain intact. |
| `python scripts/validate_bundle.py` | PASS (exit 0; 267 checks) | bundle contract and test inventory remain valid. |
| `git diff --check` | PASS; only pre-existing CRLF warning for `specs/INDEX.md` | no whitespace error in the task diff. |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/011-stakeholder-brief-client-identity-profile` | expected freshness failure only | structural/gate checks pass; the changed brief needs independent review and `--write-baseline`. |

## Acceptance and exit-criteria traceability

| Criterion | Evidence and result |
|---|---|
| FR-001 / AC-001 | The selected brief has the explicit Pearson attribute and local logo request; the neutral template has no profile attribute and requests no Pearson PNG. |
| FR-006–FR-007 / AC-006 | Browser checks prove root-level reflow at 320/390/768/1440, 200% zoom, arrow-key tabs, eight no-script panels, reduced-motion preference and non-empty print PDF. |
| FR-006 / AC-007 | Risk/control labels and the semantic coverage-table headers are asserted in browser-visible text/structure; colour is not the only meaning carrier. |
| FR-008 / AC-009 | Captured requests remain local, the selected asset is the approved local PNG and its native logo anchor has no `role="img"`; no hotlink is present. |
| FR-009–FR-010 / AC-008 | Selection/owner/authority stay in canonical `plan.md` and D-010; D-010 is requester-provided authority, not an inference from the guide. |
| FR-011 / AC-010 | Focused visual and render regressions preserve the current v2 semantics; final independent release/semantic judgment is pending. |
| T-004 exit criteria | Technical selected/unselected, offline/access-mode proof is present. The independent evaluator must still approve and the reviewed HTML baseline must be synchronized. |

## Skips, limits and residual risk

The automated render test does not certify subjective brand fidelity, a human
grayscale reading, assistive-technology announcement quality, legal authority
or semantic usefulness. D-010 is the recorded requester authority; independent
release review must judge the rendered document/evidence and ensure sources and
HTML are synchronized before the baseline is refreshed.

## Independent evaluator decision

`t004_release_review` independently re-ran the render, contract, v2, tabbed
surface, bundle and whitespace checks without editing the implementation. It
found no technical failure: local-request isolation, responsive/zoom/key/
no-script/print coverage, logo semantics, text/table state cues and D-010
authority are all accepted.

Initial decision: **needs revision** in `decision-log.md#D-016`. The only blocker was
derived-brief freshness: the HTML still states the pre-D-014 T-003/T-004/D-012
checkpoint and cannot receive a baseline. Regenerate it from D-010–D-015,
re-run render plus Human Visibility validation, refresh the baseline only after
the renewed independent review, then return this evidence pack to the same
evaluator.

## Final independent evaluator decision

`t004_release_review` rechecked the D-017-synchronized brief without editing
and returns **approve** in `decision-log.md#D-018`. It finds no remaining
technical, semantic, visual or state blocker for T-004: selected/default
isolation, local asset semantics, render-mode evidence, D-010 authority and
v2/tab/provenance preservation are accepted. It explicitly authorizes State
Keeper to write and recheck the Human Visibility baseline before the terminal
`approved → done` transition.

## Revision and resubmission

The State Keeper regenerated the HTML from D-010–D-017 and re-ran
`test_client_identity_profile_render.py`, `test_client_identity_profile_contract.py`,
`test_brief_v2_contracts.py` and `test_tabbed_brief_surface.py`; all pass. The
Human Visibility validator again reports no structural or state inconsistency,
and only the expected stale-baseline freshness failure. This resubmission is
recorded in D-017. The evaluator must recheck the current brief before State
Keeper writes a baseline or advances the task.

## Terminal validation

After D-018 approval, State Keeper ran
`python scripts/validate_human_visibility.py --consumer-root . --initiative specs/011-stakeholder-brief-client-identity-profile --write-baseline`
and then the same command without `--write-baseline`; both returned exit 0 with
no structural, state or freshness failure. D-019 records the final state,
ratchet, handoff, brief and baseline synchronization. The final complete suite
is re-run after that state record to tie this pack to the terminal worktree.

| Final command after D-019 | Result |
|---|---|
| `python scripts/test_client_identity_profile_render.py` | PASS (exit 0) |
| `python scripts/test_client_identity_profile_contract.py` | PASS (exit 0) |
| `python scripts/test_brief_v2_contracts.py` | PASS (exit 0) |
| `python scripts/test_tabbed_brief_surface.py` | PASS (exit 0) |
| `python scripts/validate_bundle.py` | PASS (exit 0; 267 checks) |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/011-stakeholder-brief-client-identity-profile` | PASS (exit 0; structural/state/freshness all clear) |
| `git diff --check` | PASS; only pre-existing CRLF warning for `specs/INDEX.md` |
