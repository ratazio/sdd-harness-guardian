# Evidence Pack: T-002

**Task:** Package local Pearson assets and foundation tokens safely  
**Builder:** platform-engineering  
**Evaluator:** identity_gate_review (independent of builder)  
**Status:** done — approved in D-011  
**Date:** 2026-08-26

## Delivered increment

- Official white Pearson logo at
  `.harness/assets/brand/pearson-logo-white.png`.
- Source: `https://plc.pearson.com/sites/pearson-corp/files/logo_w.png`.
- SHA-256:
  `8EEE1FA799766BF385A307191D38C361677D442457D7CC0F92E5F3FCCC2282F7`.
- PNG dimensions: `175 × 53`; the template preserves explicit dimensions and
  a proportional responsive width.
- `.harness/templates/stakeholder-brief.html` adds only attribute-scoped
  Pearson tokens, protected navy logo header, fallback font stack, focus
  treatment and reduced-motion rule. The generic template has no selection
  attribute, so it remains neutral and does not emit a Pearson asset request.

## Files and working-tree scope

- `.harness/assets/brand/pearson-logo-white.png`
- `.harness/templates/stakeholder-brief.html`
- `.harness/templates/plan.md` and `.harness/templates/README.md`
- `scripts/fixtures/client-identity-profile/{selected-plan.md,unselected-plan.md}`
- `scripts/test_client_identity_profile_contract.py`

The work is in the current working tree (no task-specific commit SHA was
created). It is limited to the reusable opt-in contract and its deterministic
fixture; it does not select Pearson for the generic template.

## Commands and result

| Command | Result | Oracle |
|---|---|---|
| `python scripts/test_client_identity_profile_contract.py` | PASS (exit 0) | opt-in boundary, local verified logo/dimensions/checksum, no hotlink. |
| `python scripts/test_brief_v2_contracts.py` | PASS (exit 0) | v2 template contract remains intact. |
| `python scripts/test_tabbed_brief_surface.py` | PASS (exit 0) | tabs/no-script fallback remain intact. |
| `python scripts/validate_bundle.py` | PASS (exit 0) | 267 bundle checks. |

## Acceptance and exit-criteria traceability

| Requirement / criterion | Evidence and result |
|---|---|
| FR-002, FR-006–FR-008 / AC-002, AC-003 | The focused contract verifies the local PNG's SHA-256, PNG signature and 175 × 53 dimensions; template inspection verifies the protected navy profile layer, fallback stack, focus styling, reduced motion and no remote URL. |
| FR-001 / AC-006, AC-009 | Selected/unselected fixtures and the attribute-scoped CSS prove that absence remains vendor-neutral; v2/tab checks preserve the no-script contract. Full browser/print/zoom proof is deliberately deferred to T-004. |
| FR-009–FR-010 / AC-008 | `plan.md#client-visual-profile-selection-conditional` and D-010 make the selection, owner, authority and release boundary recoverable without changing lifecycle gates. |
| T-002 exit criteria | Local asset/ratio/header path, no hotlink/filter, fallback and independent acceptance were verified; D-011 records the evaluator's approval. |

## Authority and limits

D-010 records requester authorization for the official asset, profile
implementation and selected-profile release. This task does not yet apply the
semantic variants or complete responsive/grayscale/print review; those are
T-003 and T-004.

## Independent evaluator decision

`identity_gate_review` inspected the T-002 implementation, contract output,
asset record and the evidence above without editing the implementation. Its
decision is **approve** in `decision-log.md#D-011`: the opt-in boundary,
offline asset, native link semantics, focus visibility, reduced motion and
v2/tab compatibility satisfy this task. This evaluator is distinct from
builder `platform-engineering`.

## Residual risk and handoff

No failing or skipped deterministic check remains for T-002. Semantic component
variants, visual proportionality, 320/390px, print, zoom, grayscale and final
release/isolation proof remain intentionally open in T-003/T-004; they are not
claimed by this evidence pack.
