# Evidence: T-004 — release diagnostics and regression matrix

**Builder:** Codex / implementation builder  
**Date:** 2026-08-27  
**Status:** approved — evaluate_t004_013, 2026-08-27.

## Release synchronization delivered

The release projection now agrees that T-001–T-004 are `done` under their
distinct approvals D-009, D-011, D-013 and D-016. D-014 authorized this
release task and D-015 captured the builder matrix. `tasks.md`,
`decision-log.md`, `progress.md`, `run-state.yaml` and
`stakeholder-brief.html` synchronize the terminal state without implying that
builder evidence itself granted approval.

The brief’s old fixed `tabs.length===8` guard was replaced with the approved
per-tablist initializer. This is a corrective synchronization, not a new
visual/content schema: it accepts any number of tablists, tabs, panels and
brief sections, and keeps v1 plus v2 briefs without a declared tablist outside
the tab obligation.

## Validation matrix

| Check | Result | What it establishes |
|---|---|---|
| `python scripts/test_validate_human_visibility.py` | PASS | V-001–V-005 fixture suite, including rendered/inert boundaries, redacted diagnostics, tabless/v1 controls and arbitrary per-tablist controls. |
| `python scripts/validate_bundle.py` | PASS — 267 checks | Bundle-level static regression validation. |
| `python scripts/validate_human_visibility.py --consumer-root . --initiative specs/013-brief-dom-integrity-a11y-hardening` (before refresh) | expected freshness failure only | The updated projection correctly required a new baseline; no structural or gate failure occurred. |
| same command with `--write-baseline` | PASS — baseline written | Records the reviewed-source hash set after the approved T-001–T-003 release state was synchronized. |
| same command (recheck) | PASS | No structural, gate or freshness failure. The tool still states that independent semantic/rendered review is required. |

## Diagnostic and compatibility boundary

The focused controls assert that reports name an invariant/identifier and not
fixture body text. The validator remains offline and static: it does not
execute JavaScript, score content, require an architecture block, prescribe a
tab count, or certify browser/assistive-technology behaviour. Native anchors,
source-order fallback, print expansion and reduced-motion behavior are
preserved as canonical-shell controls.

## Independent evaluation and closure

`evaluate_t004_013`, distinct from the implementation builder, independently
inspected the combined fixture realism, redaction boundary, baseline recheck
and corrected non-fixed tab initializer and approved T-004 on 2026-08-27
(D-016). A deterministic PASS was not used as a substitute for that decision.
The release gates are now true and SPEC 014 may consume the completed
DOM/tab-contract dependency.
