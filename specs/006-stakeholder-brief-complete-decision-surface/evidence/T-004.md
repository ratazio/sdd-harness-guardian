# Evidence: T-004 — scaffolding, consumer guidance and post-meeting workflow

**Initiative:** 006-stakeholder-brief-complete-decision-surface  
**Builder:** Terra 5.6 high / t4_builder  
**Evaluator:** Terra 5.6 high / t4_evaluator (distinct)  
**Task status:** done  
**Decision:** APPROVE — 2026-08-19, after one synchronization/freshness revision.

## Delivered increment

- `new_initiative.py` renders the v2 identity/date/risk/size placeholders, so a
  new scaffold is a coherent v2 draft rather than a template with unresolved
  v2 tokens. Its output still has all readiness gates false.
- Smoke coverage now asserts v2 lineage and the v2 placeholder diagnostic.
- Consumer-facing template, run-state, installation, enforcement and prompt
  guidance state the v2 order and retain historical/pinned v1 compatibility.
- The Factory executable fixture validates both a valid pinned v1 consumer and
  a v2 consumer with all required sources, distinct review metadata and a
  decision-log record confirming propagation before Tasks Ready.
- `test_post_meeting_workflow.py` is a deliberately small local fixture proving
  meeting extraction -> append-only decision log -> affected canonical source
  update -> regenerated brief -> Tasks Ready. It is not a meeting service,
  sidecar, workflow engine or HTML-only authoring path.

## Acceptance and compatibility mapping

| Requirement / AC | Result | Evidence |
|---|---|---|
| FR-007 / AC-004 | pass | Consumer guidance and the post-meeting fixture preserve the required v2 order; preliminary tasks remain non-authorizing. |
| FR-015–016 / AC-008–009 | pass | D-016 and the fixture record decision propagation through canonical sources before derived-brief refresh. |
| FR-017–018 / AC-010 | pass | Smoke creates v2; Factory validates an unchanged pinned v1 branch and a complete v2 branch. |
| FR-019 / AC-012 | pass | Manifest, template README, run-state docs, install/docs/prompt, Factory and focused regressions are synchronized. |

## Commands executed

| Command | Result |
|---|---|
| `python scripts/smoke_test_scaffolder.py` | PASS — feature and bugfix scaffold v2 coherently, reject duplicate safely and detect v2 placeholder/lineage negatives. |
| `python scripts/test_factory_guardian_fixture.py` | PASS — materializes pinned Guardian, then validates v1 and v2 consumers through the consumer wrapper. |
| `python scripts/test_post_meeting_workflow.py` | PASS — verifies the decision-log/source/regeneration order before `tasks_ready`. |
| `python scripts/test_validate_human_visibility.py` | PASS — v1/v2 validator and migration/privacy fixtures remain green. |
| `python scripts/test_brief_v2_contracts.py` | PASS — lifecycle/lineage contract remains green. |
| `python scripts/validate_bundle.py` | PASS — 267 checks. |
| `git diff --check` | PASS — no whitespace errors. |

## Scope and residual risk

No release, publish, tag, hosted service, meeting-platform integration,
permanent agent, sidecar or new orchestration layer was added. Consumer
repositories remain owners of meeting-decision extraction, canonical source
edits and brief regeneration. Rendered and cross-role evaluation remains T-005
scope.

## Builder handoff

The task has moved `in_progress -> needs_evaluation`; it must not become `done`
without the distinct `t4_evaluator` decision and State Keeper synchronization.
Evaluator focus: verify that the v2 scaffold is rendered, v1 is genuinely
pinned/accepted rather than faked, the Factory wrapper reaches both paths, and
the post-meeting fixture does not grant Tasks Ready before source propagation
and brief regeneration.

## Revision 1 — synchronization/freshness

The evaluator requested only the material source-to-brief/state synchronization.
The Builder applied `needs_evaluation -> needs_revision -> in_progress ->
needs_evaluation`: added D-016 to the regenerated brief and coverage view,
aligned T-004 status/readiness history and handoff recipient, renewed the v2
baseline after the 006 validator passed, and reran every listed regression. No
Evaluator decision is claimed by this revision.

## Evaluator record

The distinct evaluator approved T-004 after independently re-running the 006
validator, scaffolder smoke, Factory v1/v2 fixture, post-meeting fixture,
validator unit suite, lifecycle contracts, the 267-check bundle validation and
`git diff --check`. The refreshed brief/baseline/handoff resolved the only
revision findings. No release/tag/publish, service or additional layer was
introduced.
