# Evidence: T-002 — progressive v2 template and populated decision example

**Builder:** Terra 5.6 high / t2_builder  
**Evaluator:** Terra 5.6 high / t2_evaluator (distinct; pending)  
**Task status:** needs_evaluation  
**Scope boundary:** canonical template and populated initiative-006 HTML only;
no production validator, Factory, scaffolder, consumer documentation/prompt, or
release surface was changed.

## Delivered increment

- Replaced the canonical shell with `data-harness-brief-design="v2"`, retaining
  the v1 visual tokens, executive header, decision ask, snapshot and responsive
  paper layout.
- Added eight progressive views: value/scope, architecture, impact, execution,
  validation, evolution, decision and coverage. Each view is source-provenanced
  by `data-source`, `data-source-section` and `data-coverage`; the coverage view
  has the required readable source/heading/disposition table. No JSON or sidecar
  was added.
- Promoted the 006 brief to v2, retained its L/high source-backed architecture
  diagram and text equivalent, and corrected the visible state to show T-002
  awaiting evaluation with v2 coverage/Human Visibility/Tasks Ready unapproved.
- Added D-013, resolving Q-002 with native anchors and `details`: JavaScript only
  collapses details after load, no-script leaves them open, and print uses a CSS
  fallback that reveals closed details.

## Source-to-view coverage

| Source set | Rendered views | Disposition |
|---|---|---|
| `spec.md` | snapshot, scope, impact, validation, coverage | synthesized / represented |
| `plan.md`, `impact-map.md` | architecture, impact, execution | synthesized / source-backed diagram |
| `tasks.md`, `run-state.yaml` | execution, snapshot, evolution | represented; task authority explicit |
| `validation-plan.md` | validation, coverage | synthesized / represented |
| `decision-log.md`, `progress.md`, `ratchet.md`, handoff | evolution, decision, coverage | represented, including D-013 and justified ratchet N/A |

The populated HTML retains the human coverage register for individual principal
heading groups. The new template supplies the same table shape and block-local
provenance attributes for future briefs.

## Validation executed

| Check | Result |
|---|---|
| Static HTMLParser contract for both template and 006 | PASS: eight views, v2 marker, provenance blocks, native details, no JSON index |
| Playwright / Edge at 1440×900 | PASS: `scrollWidth == innerWidth`; all deep details collapse with JS |
| Playwright / Edge at 390×844 | PASS: `scrollWidth == innerWidth`; all deep details collapse with JS |
| Playwright / Edge with JavaScript disabled at 390×844 | PASS: no horizontal overflow; all details remain open/readable |
| Playwright keyboard / nav and print media | PASS: focus reaches native anchor/summary controls; every closed-detail child is visible in print media |
| Render artifacts | Created locally under `%TEMP%/sdd-t002-render/` (desktop PNG, narrow PNG, print PDF); inspected desktop hierarchy and narrowed layout through Playwright dimensions |
| `python scripts/validate_bundle.py` | Initial pre-revision run passed the legacy shell. After truthful v2 cleanup it correctly fails seven v1-only template expectations; see the recorded limitation below. |
| `git diff --check` | PASS |

## Known limits and evaluator focus

- The production v2 DOM/freshness validator belongs to T-003; this task did not
  change it. The unchanged v1-only bundle validator fails truthfully against the
  v2 template until T-003 supplies its lineage-aware branch.
- The HTML reports coverage review and Human Visibility as pending. It does not
  claim semantic or independent rendered approval.
- The evaluator should inspect the template/example against the eight views,
  source claims, task-draft authority boundary, D-013 no-script/print behavior,
  and the rendered local checks. The Builder must not approve or mark the task
  done.

## Revision after evaluator request

The Builder applied the requested `needs_evaluation -> needs_revision ->
in_progress -> needs_evaluation` correction. The 006 HTML is now wholly v2/T-002
focused: T-001 planning/prototype text is removed; status is
`needs_evaluation`; and `brief_coverage_ready`, Human Visibility and Tasks Ready
remain false. D-013/Q-002 is represented in the header, impact/unknowns,
evolution ledger, decision and coverage register.

Provenance now uses one `data-source` per material element with no CSV source
lists. The human register identifies stable targets and gives explicit reasons
for each `not_applicable`; material core source groups are synthesized or
represented, never `link_only`. The v2 design standard now documents lineage,
historical v1 preservation, source-local provenance and the native-details
baseline.

Rendered artifacts are retained in `evidence/rendered/T-002/` with their local
reproduction notes. The static provenance check and Playwright desktop/narrow,
no-script, keyboard and print checks pass. `git diff --check` passes.

### Legacy-validator limitation

The unchanged production validator is intentionally v1-only: it requires the
literal v1 marker, old raw placeholders and the four-source v1 shell. A truthful
v2 template therefore fails that legacy design-contract branch, and the 006 v2
brief also correctly fails because its v2 Human Visibility gate is false.
No compatibility comment, fake marker, hidden hook or validator edit was used.
T-003 owns the lineage-aware validator/migration work. This is a recorded
limitation, not a pass or approval.

### Second evaluator revision

The permitted coverage values are exclusively `represented`, `synthesized`,
`not_applicable` and `link_only`. A gap is a blocking finding and is never a
disposition. The builder removed `gap` as a template/example disposition and
corrected the stale legacy-validator-green wording, then transitioned
`needs_revision -> in_progress -> needs_evaluation` for distinct review.

## Independent evaluation

**Evaluator:** Terra 5.6 high / t2_evaluator  
**Decision:** `approve` after two `request_revision` rounds.  
**Final findings:** none blocking. The evaluator confirmed eight views, one
canonical source per provenance block, valid coverage dispositions, no duplicate
IDs, truthful v2/T-002 state, D-013 propagation, persistent render artifacts and
the accessible native-details baseline.  
**Accepted limitation:** the unchanged v1-only production validator fails until
T-003 adds the lineage-aware branch; no fake compatibility marker or validator
edit was accepted.  
**State Keeper:** recorded `approved -> done`; D-014 records the v2
coverage/rendered review and gate transition.

### V-011/M-001 terminal render correction

T-005 found that the dense decision and coverage tables did not overflow the
390px page but compressed words too far to be readable. A minimal T-002-owned
CSS correction gives their cells a 10rem narrow-screen minimum and permits
horizontal scrolling inside each table. Desktop, no-script and print behavior
remain intact. `narrow-table-fix.png` and `narrow-table-fix-no-script.png` are
retained in `evidence/rendered/T-002/`.

Playwright confirms no global overflow at 390px, local scroll for both tables,
open details without script and no desktop overflow. The HTML change leaves the
v2 baseline stale; it awaits distinct rendered recheck and has not renewed the
baseline or inferred another approval.

Focused checks after the correction: `python scripts/test_brief_v2_contracts.py`
and `python scripts/test_validate_human_visibility.py` pass; `git diff --check`
passes. The initiative validator has no structural or state failure and fails
only its expected freshness check until that distinct rendered recheck writes a
new baseline.
