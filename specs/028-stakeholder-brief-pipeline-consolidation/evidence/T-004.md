# Evidence — T-004: revisão renderizada e validações alcançáveis

**Builder:** Codex (builder)  
**Evaluator:** pipeline_audit (independente)  
**Verdict:** APPROVE — 2026-09-02

## Inventário de influência efetiva

| Componente | Disposição | Papel efetivo após T-004 |
|---|---|---|
| `render_stakeholder_brief.py` | ativo | promove somente candidate v3 que preserva o skeleton local; finalização exige record de review HTTP. |
| `validate_brief_candidate_inheritance.py` | ativo no boundary | chamado pelo renderer para promoção normal e `--render-unapproved`; não julga narrativa. |
| `validate_human_visibility.py` | ativo | valida a alegação de Human Visibility e agora exige URL loopback e ambiente no record quando o review de qualidade é requerido. |
| `architecture_visual_contract.py` | ativo | importado pelos validadores; preserva disposição visual/arquitetural, não escolhe o diagrama. |
| `executive-brief-composition` + Brief Experience Composer | ativo e nomeado | autoria do candidate a partir do skeleton copiado. |
| `executive-brief-experience-review` + Executive Brief Reviewer | ativo e nomeado | review pré-skeleton de construção. |
| `rendered-brief-decision-review` | ativo e nomeado | review independente do HTML servido em HTTP. |
| `validate_semantic_review_record.py` | opcional justificado | envelope adicional; não entra no lifecycle nem cria gate duplicado. |

## Correções e prova

| Finding | Correção mínima | Prova |
|---|---|---|
| F-004-01: a herança física não alcançava a entrega | `candidate_skeleton_inheritance_error()` no renderer, antes de ambas as promoções v3. | `python scripts/test_renderer_skeleton_boundary.py` → PASS. |
| F-004-02: review final podia não provar a superfície HTTP | finalizador e Human Visibility exigem `Preview URL: http://127.0.0.1...` e `Preview environment:` no evidence. | `python scripts/test_post_render_finalization.py` e `python scripts/test_quality_review_preview_binding.py` → PASS. |
| F-004-03: papéis/skills eram descobertos indiretamente | registrar ambos os agents no manifest/AGENTS e vinculá-los no lifecycle. | inspeção de `manifest.yaml`, `.harness/AGENTS.md` e `.harness/workflows/sdd-lifecycle.md`. |

O renderer continua permitindo `--render-unapproved` quando existe candidate
rastreável; esse caminho não seta Human Visibility, Tasks Ready ou entrega.
Nenhum gerador Markdown→HTML, score visual ou gate de aprovação do usuário foi
introduzido.

## Validações executadas

```text
python scripts/test_quality_review_preview_binding.py  PASS
python scripts/test_post_render_finalization.py         PASS
python scripts/test_renderer_skeleton_boundary.py       PASS
python scripts/test_unapproved_brief_render.py          PASS
python scripts/test_validate_human_visibility.py        PASS (execução do builder)
```

## Próximo passo seguro

Concluir a recomposição R4 do M003 e então executar a matriz fresca M001–M008
sob os mesmos boundaries, com revisão visual independente em HTTP.
