# Evidence — T-003 composition integrity and reviewed editorial exception

**Builder:** `/root`  
**Evaluator:** `/root/spec025_independent_review` — approve after correction of schema/visibility and CLI review.  
**Date:** 2026-09-01

## Implemented boundary

`scripts/editorial_exceptions.py` is opt-in (`data-composition-contract="v3"`)
and finds only current mechanical omissions: task projection, validation
projection and explicit unresolved skeleton slot. It never scores prose,
hierarchy, aesthetics or materiality.

`render_stakeholder_brief.py` keeps the existing exact pre-render review
(candidate SHA-256, composition manifest, distinct author/reviewer,
provenance, lifecycle and composed identity) before it considers a v3
exception. The explicit `--allow-reviewed-editorial-exceptions` mode then
requires every live finding to have exactly one visible
`#composition-exceptions` entry and matching review-record fields; expiry is
enforced. All regular structural and Pearson policy checks run afterwards.

`validate_human_visibility.py` reports an open rendered exception as visible
decision debt and refuses any state that tries to set Human Visibility or Tasks
Ready true while it remains open.

## Commands and results

```text
python scripts/test_editorial_exceptions.py                 PASS (including real renderer CLI)
python scripts/test_render_stakeholder_brief.py             PASS
python scripts/test_brief_composition_contract.py           PASS
python scripts/test_validate_human_visibility.py            PASS
python scripts/test_architecture_visual_contract.py         PASS
python scripts/test_pearson_brief_policy.py                 PASS
python scripts/validate_bundle.py                           PASS (272 checks)
```

The exception fixture has positive coverage plus negative cases for normal
mode, missing owner, expired record and a visible/record ID mismatch.

## Remaining proof

T-004 must run the same route on a fresh M-003 disposable consumer and obtain
its distinct rendered review; no historical SPEC 024 reference is used as a
delivery artifact.
