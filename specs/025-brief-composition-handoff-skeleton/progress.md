# Progress — SPEC 025

## Checkpoint 2026-09-01 — systemic gap isolated

The M-003 audit showed five source tasks and seven validation proofs missing
from a visually polished reference. Three bounded audits found canonical v2
composition in policy/workflow/template, while the SPEC 024 laboratory bypassed
it through a hard-coded compact CASES list.

SPEC 025 operationalizes the existing composition path; it does not create a
competing renderer or rewrite the laboratory. The plan awaits independent SPEC
review before task authorization.

## Checkpoint 2026-09-01 — SPEC approved, lifecycle corrected

An independent reviewer returned one REVISE: the first plan reversed render
review/promotion, left skeleton identity undefined and mixed discovery with the
four coverage dispositions. D-025-002 repaired those points. Re-review
approved the SPEC and plan. Existing guarded-promotion, composition-parity and
bundle checks pass; no delivery pipeline or laboratory artifact has yet changed.

## Checkpoint 2026-09-01 — stakeholder brief rendered and independently reviewed

The editorial map received a distinct coverage approval (D-025-004). A
desktop-first candidate was composed from the sources, then independently
reviewed and hash-bound (D-025-005). The first promoted artifact received a
post-render REVISE for an outdated candidate footer, omitted D-025-005 and an
out-of-order impact flow; D-025-006 records that finding. The source and
candidate were corrected, re-attested (D-025-007), refreshed through the
guarded renderer and independently approved at post-render.

`stakeholder-brief.html` now presents eight real `?view` subpages: scope,
architecture, impact, execution, validation, evolution, decision and
coverage. It contains the task contracts, added/adjusted/removed surfaces,
deterministic versus human assurance boundary, source-backed topology/zoom,
seven validations, rollback and laboratory limit. The post-render evidence is
bound to the pre-finalization rendered snapshot in
`evidence/post-render-review.md`.

The lifecycle truth remains deliberately conservative: the brief has been
rendered and reviewed, but Human Visibility, Tasks Ready, delivery and
implementation authorization remain false pending a separate meeting decision
and decision propagation.

## Checkpoint 2026-09-01 — controlled editorial-exception path implemented

D-025-008 records the requester’s execution authority. T-001 through T-003
are complete and independently reviewed: the canonical handoff/template now
uses a single reviewed-exception schema; the v3 opt-in checks only current
task/validation/slot projection omissions; the renderer refuses by default,
then accepts a candidate only under explicit CLI mode plus a visible,
hash/manifest-bound, owned, expiring exception. Hidden/inert entries, schema
drift, stale findings, expired entries and mismatched IDs are refused.

The unchanged hard boundary is deliberate: composed identity, Candidate
SHA-256, composition manifest, distinct reviewer, provenance, lifecycle,
structural and Pearson policy failures never become editorial exceptions. The
fresh M-003 composition and distinct rendered review in T-004 are the remaining
proof before the initiative can be declared complete.

## Checkpoint 2026-09-01 — M-003 r9 pilot approved; SPEC complete

The r9 M-003 root was promoted only after D-250 bound its exact candidate hash
and composition manifest. An independent reviewer returned REVISE twice for
material source-to-visible-authority drift, then APPROVE after the candidate
was recomposed. The final pilot recovered all five task contracts, seven proof
paths, privacy/impact, decisions and rollback without touching SPEC 024
references. Post-render review is recorded in the pilot and the bundle
regressions pass. No reviewed editorial exception was necessary in this case;
the separate v3 CLI fixture proves the low-impact, visible exception path.

## Checkpoint 2026-09-01 — visual construction contract reopened

The requester rejected the delivered skeleton correctly: it was a lifecycle
fixture, not a visual commitment. Its `plan.md` map named visual forms but did
not define the storytelling, relations or fields those forms must contain. D-
025-010 reopens the initiative for T-005. The correction remains generic: a
single `plan.md` contract and a non-promotable instance, rather than a second
source of truth or an M-003-specific renderer.

## Checkpoint 2026-09-01 — visual construction contract corrected and reviewed

T-005 replaced the text-and-anchor skeleton with a complete `executive-brief-v3`
contract.  The generic template and the selected Pearson instance now share
eight routed subpages, component families and visible slots.  The plan defines
the narrative question, sources, relationship model, visual form, repetition
and explicit absence for every route; it also makes agent authorship of final
HTML/CSS/JS non-negotiable.  Scripts remain structural/provenance/promotion
guards only.

The independent evaluator approved the change after the route, panel, ID,
`aria-controls`, component-family, task/proof-slot and source-map parity check
passed.  Evidence is in `evidence/T-005.md`; D-025-011 records the decision.
The task is technically complete, but the skeleton is still not a brief,
not promotable and not a meeting approval.  Human Visibility and Tasks Ready
remain false until a stakeholder inspects a composed initiative-specific brief.

## Checkpoint 2026-09-01 — stakeholder accepted skeleton direction; T-006 started

The requester accepted the skeleton as the base artifact, not merely an
example.  D-025-012 authorizes a fresh SPEC 025 candidate copied from it, with
no promotion.  The contract will distinguish a source-selected solution
landscape (changed/preserved/out-of-scope surfaces) from a separate contextual
relationship flow when both are material.  This expands the generic editorial
choice; it does not make software architecture or a fixed topology mandatory.

## Checkpoint 2026-09-01 — composed candidate reviewed, not promoted

T-006 copied the approved skeleton into a new SPEC 025 candidate and filled
the eight routes from canonical sources.  It declares the skeleton path/hash,
recovers six task dossiers and ten proof dossiers, and uses two distinct
architecture representations appropriate to this workflow: an ecosystem
landscape and the source-to-candidate control flow.  The independent evaluator
approved the composition; evidence is `evidence/T-006.md` and D-025-013.

No `stakeholder-brief.html` changed.  Human Visibility and Tasks Ready stay
false: the next safe operation is stakeholder inspection of the candidate,
not automatic promotion.
